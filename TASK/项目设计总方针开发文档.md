# yFeiEye 50路摄像头AI检测系统 - 项目设计总方针开发文档

**文档版本**: v2.0  
**创建日期**: 2025-10-24  
**目标**: 实现50路摄像头的边缘AI实时检测、前端管理和告警处理

---

## 📊 第一部分：系统架构与端口配置

### 1.1 整体架构图

```
┌────────────────────────────────────────────────────────────────────┐
│                    yFeiEye AI 智眼平台                          │
│                                                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │   WEB    │  │  DEVICE  │  │   AI     │  │  VIDEO   │         │
│  │  前端UI  │  │  后端网关 │  │ 模型训练  │  │ 视频转发  │         │
│  │  :8099   │  │  :48080  │  │  :5000   │  │  :6000   │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
│       ↓              ↓             ↓             ↓                │
│  ┌────────────────────────────────────────────────────┐          │
│  │              基础设施层                              │          │
│  │  PostgreSQL | MinIO | ZLMediaKit | Node-RED        │          │
│  │    :5432    | :9000 |   :80,:1935|  :1880          │          │
│  └────────────────────────────────────────────────────┘          │
│                                                                    │
│  ┌────────────────────────────────────────────────────┐          │
│  │     边缘计算层（本次开发重点）                         │          │
│  │  TaskManager(:7000) → 50个TASK实例(C++)            │          │
│  │  HTTP API管理器        实时AI推理                    │          │
│  └────────────────────────────────────────────────────┘          │
└────────────────────────────────────────────────────────────────────┘
```

### 1.2 端口配置总览（⚠️ 重要）

| 模块 | 端口 | 协议 | 用途 | 状态 |
|------|------|------|------|------|
| **WEB（前端）** | `8099` | HTTP | Vue3前端界面 | ✅ 运行中 |
| **DEVICE（网关）** | `48080` | HTTP | Spring Cloud Gateway | ✅ 运行中 |
| **AI（训练）** | `5000` | HTTP | 模型训练/导出 | ✅ 运行中 |
| **VIDEO（转发）** | `6000` | HTTP | RTSP转发管理 | ✅ 运行中 |
| **TaskManager** | `7000` | HTTP | TASK任务管理 | ❌ 待开发 |
| **ZLMediaKit** | `80` | HTTP | HTTP-FLV播放 | ✅ 运行中 |
| **ZLMediaKit** | `1935` | RTMP | RTMP推流接收 | ✅ 运行中 |
| **Node-RED** | `1880` | HTTP | 规则引擎 | ✅ 运行中 |
| **PostgreSQL** | `5432` | TCP | 数据库 | ✅ 运行中 |
| **MinIO** | `9000` | HTTP | 对象存储 | ✅ 运行中 |

### 1.3 API路由映射

**前端代理配置**（`WEB/.env.development`）:
```javascript
VITE_PORT = 8099
VITE_PROXY = [
  ["/api/v1/buckets", "http://localhost:9000/api/v1/buckets"],    // MinIO
  ["/dev-api/nodeRed", "http://127.0.0.1:1880"],                  // Node-RED
  ["/dev-api/task", "http://127.0.0.1:7000"],                     // TaskManager（待添加）
  ["/dev-api", "http://127.0.0.1:48080/admin-api"]                // DEVICE网关
]
```

**后端网关路由**（`DEVICE/iot-gateway/src/main/resources/application.yaml`）:
```yaml
spring.cloud.gateway.routes:
  - id: video-admin-api
    uri: lb://video-server
    predicates:
      - Path=/admin-api/video/**
    
  - id: model-admin-api
    uri: lb://model-server
    predicates:
      - Path=/admin-api/model/**
    
  - id: device-admin-api
    uri: lb://device-server
    predicates:
      - Path=/admin-api/device/**
  
  # ⚠️ 需要新增TaskManager路由
  - id: task-admin-api
    uri: http://localhost:7000
    predicates:
      - Path=/admin-api/task/**
    filters:
      - StripPrefix=2
```

---

## 📝 第二部分：各模块功能详解

### 2.1 WEB模块（Vue3前端）

**端口**: `8099`  
**技术栈**: Vue3 + TypeScript + Ant Design Vue + Vite

#### 核心页面

| 页面路径 | 功能 | API调用 | 状态 |
|---------|------|---------|------|
| `/camera` | 摄像头管理 | `GET /video/camera/list`<br>`POST /video/camera/device/{id}/stream/start`<br>`POST /video/camera/device/{id}/stream/stop` | ✅ 完成 |
| `/dataset` | 数据标注 | `GET /model/dataset/list`<br>`POST /model/dataset/create` | ✅ 完成 |
| `/train` | 模型训练 | `POST /model/train/start`<br>`GET /model/train/status` | ✅ 完成 |
| `/rulechains` | 规则引擎 | Node-RED集成 | ✅ 完成 |
| `/ai-task` | **AI任务管理** | `POST /task/start`<br>`GET /task/list` | ❌ **待开发** |
| `/video-wall` | **视频墙** | `GET /task/list`<br>WebSocket告警 | ❌ **待开发** |

#### 视频播放器组件

**文件**: `WEB/src/components/VideoPlayer/DialogPlayer.vue`  
**播放器**: Jessibuca（支持HTTP-FLV）  
**功能**:
- HTTP-FLV视频播放
- PTZ云台控制
- 播放地址切换

---

### 2.2 DEVICE模块（Java Spring后端网关）

**端口**: `48080`  
**技术栈**: Java 17 + Spring Boot + Spring Cloud Gateway

#### 核心API

```java
// 设备管理
GET    /admin-api/device/list              // 设备列表
POST   /admin-api/device/create            // 创建设备
PUT    /admin-api/device/update/{id}       // 更新设备
DELETE /admin-api/device/{id}              // 删除设备

// ⚠️ 告警回调（TASK可以直接使用）
POST   /admin-api/device/alarm             // 设备告警回调
```

**告警回调请求示例**:
```json
POST http://localhost:48080/admin-api/device/alarm
Content-Type: application/json

{
  "device_id": "camera_001",
  "alarm_type": "ai_detection",
  "alarm_level": "warning",
  "alarm_content": "检测到未佩戴安全帽",
  "detections": [
    {
      "class_id": 0,
      "class_name": "no_helmet",
      "confidence": 0.85,
      "bbox": {"x": 100, "y": 200, "w": 50, "h": 100}
    }
  ],
  "region_id": "entrance",
  "timestamp": "2025-10-24 10:30:00"
}
```

**Node-RED规则引擎**（端口1880）:
- 告警规则配置
- 企业微信通知
- 钉钉机器人
- 邮件/短信通知
- 数据转发（Kafka/MQTT）

---

### 2.3 VIDEO模块（Python Flask视频转发）

**端口**: `6000`  
**技术栈**: Python 3.8+ + Flask + FFmpeg + ONVIF

#### 核心功能

```python
# RTSP转发管理
POST   /admin-api/video/camera/device/{device_id}/stream/start   # 启动转发
POST   /admin-api/video/camera/device/{device_id}/stream/stop    # 停止转发
GET    /admin-api/video/camera/device/{device_id}/stream/status  # 查询状态

# ONVIF设备管理
GET    /admin-api/video/camera/discovery                         # 设备发现
POST   /admin-api/video/camera/device/{device_id}/ptz            # PTZ控制

# 截图功能（用于数据标注）
POST   /admin-api/video/camera/device/{device_id}/rtsp/start     # 启动截图
```

**数据模型**（`VIDEO/models.py`）:
```python
class Device(db.Model):
    id = db.Column(db.String(100), primary_key=True)
    name = db.Column(db.String(100))
    source = db.Column(db.Text)           # RTSP地址
    rtmp_stream = db.Column(db.Text)      # RTMP推流地址
    http_stream = db.Column(db.Text)      # HTTP-FLV播放地址
    ip, port, username, password          # 连接信息
    mac, manufacturer, model              # 设备信息
    support_move, support_zoom            # PTZ能力
    enable_forward = db.Column(db.Boolean)  # 是否启用转发
```

**⚠️ 改造方案**:
- 保留ONVIF、PTZ、截图功能
- 新增字段: `ai_task_id`, `ai_enabled`, `ai_model_path`
- 前端可选择: 使用VIDEO转发（无AI）或启动TASK（有AI）

---

### 2.4 AI模块（Python Flask模型训练）

**端口**: `5000`  
**技术栈**: Python 3.8+ + Flask + YOLOv8 + ONNX Runtime

#### 核心功能

```python
# 模型训练
POST   /admin-api/model/train/start       # 启动训练
POST   /admin-api/model/train/stop        # 停止训练
GET    /admin-api/model/train/status      # 训练状态
GET    /admin-api/model/train/logs        # 训练日志

# 模型导出
POST   /admin-api/model/export/onnx       # 导出ONNX格式 ⚠️ TASK使用
POST   /admin-api/model/export/torchscript
GET    /admin-api/model/export/list

# 推理测试
POST   /admin-api/model/inference/predict # 单张图片检测
GET    /admin-api/model/inference/list    # 推理任务列表
```

**与TASK的关系**:
```
AI模块（科学家）              TASK模块（工人）
    ↓                          ↓
1. 训练YOLOv8模型         1. 加载onnx模型
2. 导出SafeHat.onnx  →   2. 实时视频推理
3. 测试单张图片           3. 处理50路视频流
4. 存储到MinIO           4. 生产部署
```

---

### 2.5 TASK模块（C++边缘AI推理）⭐ 开发重点

**技术栈**: C++17 + ONNX Runtime + OpenCV + FFmpeg + Glog

#### 已完成功能 ✅

```cpp
✅ RTSP视频解码（FFmpeg）
✅ YOLOv11推理引擎（ONNX Runtime CPU）
✅ 异步推理（4线程池）
✅ 实时画框显示（OpenCV窗口）
✅ 告警HTTP回调（cpp-httplib）
✅ 置信度过滤
✅ 告警冷却机制（防止频繁告警）
✅ RTSP自动重连
✅ 配置文件解析（INI格式）
✅ RTMP推流（已实现，HTTP-FLV播放1秒延迟）
✅ 区域配置解析（ConfigParser支持[regions]段，解析JSON多边形）
```

#### 需要开发功能 ❌

```cpp
❌ 区域过滤逻辑（0.5天）
   └─ cv::pointPolygonTest() 判断检测框中心点是否在多边形区域内
   └─ 只有在区域内的检测才触发告警

❌ TaskManager进程管理器（2-3天）⭐⭐⭐⭐⭐ 最重要
   └─ HTTP API服务器（cpp-httplib，端口7000）
   └─ 管理50个TASK实例（启动/停止/查询/列表）
   └─ 配置文件生成器（JSON→INI）
   └─ 心跳监控

❌ 多模型动态加载（1天）
   └─ 运行时切换模型，无需重启

❌ 类别文件加载（0.5天）
   └─ 支持中文标签（读取classes.txt）

❌ 资源释放完善（0.5天）
   └─ 防止内存泄漏，确保RTSP断线重连时正确释放
```

#### 文件结构

```
TASK/
├── src/
│   ├── Detech.cpp/h              # 主程序（视频处理+AI推理）
│   ├── Yolov11Engine.cpp/h       # YOLO推理引擎
│   ├── RTMPEncoder.cpp/h         # RTMP推流（已完成）
│   ├── AlarmCallback.cpp/h       # 告警回调
│   ├── ConfigParser.cpp/h        # 配置解析
│   ├── Config.h                  # 配置结构体
│   ├── ThreadPoolExecutor.h      # 线程池
│   ├── Utils.h                   # 工具函数
│   └── TaskManager.cpp/h         # ❌ 待开发
├── config/
│   ├── test.ini                  # 测试配置
│   ├── task1.ini ~ task50.ini    # ❌ 50路配置（待生成）
├── models/
│   ├── yolov11n.onnx            # YOLO模型
│   └── SafeHat.onnx             # 安全帽检测模型
├── CMakeLists.txt               # 编译配置
└── build/Release/
    ├── TASK.exe                 # 可执行文件
    └── TaskManager.exe          # ❌ 待编译
```

---

## 🎯 第三部分：完整业务流程

### 3.1 数据流示意图

```
┌─────────────────────────────────────────────────────────────┐
│ 阶段1：模型准备（AI模块）                                       │
│ 数据标注 → YOLOv8训练 → 导出SafeHat.onnx → 存储到MinIO       │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段2：任务配置（WEB前端）                                      │
│ 1. 选择摄像头（RTSP地址）                                      │
│ 2. 选择模型（SafeHat.onnx）                                   │
│ 3. 配置检测区域（多边形ROI）                                   │
│ 4. 配置告警规则（阈值、冷却时间）                              │
│ 5. 点击"启动AI检测"                                          │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段3：任务启动（TaskManager）                                 │
│ POST /dev-api/task/config/generate  → 生成config/task1.ini  │
│ POST /dev-api/task/start            → 启动TASK实例          │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段4：实时推理（TASK实例）                                     │
│ RTSP拉流 → FFmpeg解码 → YOLO推理 → OpenCV画框              │
│     ↓           ↓           ↓           ↓                   │
│  25fps      H.264帧    检测结果    带框画面                   │
│                                      ↓                       │
│                               RTMP推流编码                    │
│                                      ↓                       │
│                            ZLMediaKit(:1935)                │
│                                      ↓                       │
│                            HTTP-FLV(:80)                    │
│                                      ↓                       │
│                            前端播放(1秒延迟)                  │
│                                                             │
│ 并行流程：区域过滤 → 告警判断 → HTTP POST告警                  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 阶段5：告警处理（DEVICE + Node-RED）                           │
│ TASK → POST /admin-api/device/alarm                         │
│ DEVICE接收 → Node-RED规则引擎                                │
│           ↓                                                 │
│ WebSocket推送前端（实时）                                      │
│ PostgreSQL存储（历史）                                         │
│ 企业微信通知（@管理员）                                         │
│ 钉钉机器人（群消息）                                           │
│ 邮件/短信（严重告警）                                          │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 配置文件完整示例

```ini
# TASK/config/task1.ini

[task]
task_id=1
task_name=entrance_monitor

[video]
source=rtsp://admin:Admin123@192.168.1.100:554/stream1
width=1920
height=1080
fps=25
enable=true

[model]
model_path=models/SafeHat.onnx
classes_path=models/SafeHat_cn.txt
confidence_threshold=0.5
nms_threshold=0.45
threads=1

[rtmp]
enable=true
rtmp_url=rtmp://localhost:1935/live/camera_1
fps=25
enable_draw=true

[alarm]
enable=true
hook_url=http://localhost:48080/admin-api/device/alarm
confidence_threshold=0.6
cooldown_time=10

[regions]
entrance=[[100,200],[300,200],[300,400],[100,400]]
parking=[[500,500],[800,500],[800,700],[500,700]]

[features]
enable_rtmp=true
enable_draw=true
enable_alarm=true
```

---

## 🚀 第四部分：开发路线图（8-12天）

### 阶段1：TASK模块补全核心功能（1天）⚡ 最快见效

#### Day 1上午：区域过滤逻辑（0.5天）

**任务**: 实现点在多边形内判断

**文件**: `TASK/src/Detech.cpp`

**代码实现**:
```cpp
// 1. 添加区域过滤函数
bool isInAlarmRegion(const Detection& det, const std::vector<cv::Point>& region) {
    // 计算检测框中心点
    float centerX = det.x + det.w / 2.0f;
    float centerY = det.y + det.h / 2.0f;
    cv::Point2f center(centerX, centerY);
    
    // OpenCV点在多边形内判断
    double result = cv::pointPolygonTest(region, center, false);
    return result >= 0;  // >= 0 表示在区域内
}

// 2. 在告警逻辑中使用
std::vector<Detection> alarmDetections;
for (const auto& det : detections) {
    if (det.confidence < _config.alarmConfidenceThreshold) {
        continue;  // 置信度不够
    }
    
    // 遍历所有配置的区域
    bool inRegion = false;
    for (const auto& region : _config.regions["default"]) {
        if (isInAlarmRegion(det, region)) {
            inRegion = true;
            break;
        }
    }
    
    if (inRegion) {
        alarmDetections.push_back(det);
    }
}

// 3. 触发告警
if (!alarmDetections.empty()) {
    _alarmCallback->sendAlarm(taskId, alarmDetections, "region_main", timestamp);
}
```

**验收标准**:
```bash
# 1. 配置多个区域
[regions]
entrance=[[100,100],[500,100],[500,400],[100,400]]
parking=[[600,100],[900,100],[900,400],[600,400]]

# 2. 测试
- 在entrance区域内检测到人 → 触发告警 ✅
- 在parking区域外检测到人 → 不触发告警 ✅
- 完全在区域外 → 不触发告警 ✅
```

#### Day 1下午：资源释放完善（0.5天）

**文件**: `TASK/src/Detech.cpp`

**代码实现**:
```cpp
void Detech::releaseResources() {
    LOG(INFO) << "[CLEANUP] Releasing all resources...";
    
    // 1. 释放RTMP编码器
    if (_rtmpEncoder) {
        _rtmpEncoder->release();
        delete _rtmpEncoder;
        _rtmpEncoder = nullptr;
    }
    
    // 2. 释放YOLO引擎
    if (_yoloEngine) {
        delete _yoloEngine;
        _yoloEngine = nullptr;
    }
    
    // 3. 释放FFmpeg资源
    if (_videoCtx) {
        avformat_close_input(&_videoCtx);
        _videoCtx = nullptr;
    }
    
    if (_codecCtx) {
        avcodec_free_context(&_codecCtx);
        _codecCtx = nullptr;
    }
    
    // 4. 释放告警回调
    if (_alarmCallback) {
        delete _alarmCallback;
        _alarmCallback = nullptr;
    }
    
    LOG(INFO) << "[CLEANUP] All resources released successfully";
}

// 在析构函数中调用
Detech::~Detech() {
    releaseResources();
}
```

---

### 阶段2：TaskManager进程管理器（2-3天）⭐⭐⭐⭐⭐ 核心

#### Day 2-3：HTTP API实现

**新建文件**: `TASK/src/TaskManager.cpp`

**完整代码**:
```cpp
#include <httplib.h>
#include <nlohmann/json.hpp>
#include <map>
#include <memory>
#include <thread>
#include "Detech.h"
#include "ConfigParser.h"

using json = nlohmann::json;

class TaskManager {
private:
    std::map<int, std::unique_ptr<std::thread>> taskThreads;
    std::map<int, std::shared_ptr<Config>> taskConfigs;
    std::map<int, std::string> taskStatus;  // "running", "stopped", "error"
    httplib::Server server;
    std::mutex mtx;

public:
    TaskManager(int port = 7000) {
        LOG(INFO) << "[TaskManager] Initializing on port " << port;
        setupRoutes();
    }
    
    void setupRoutes() {
        // POST /task/start - 启动任务
        server.Post("/task/start", [this](const httplib::Request& req, httplib::Response& res) {
            try {
                auto json = nlohmann::json::parse(req.body);
                int taskId = json["task_id"];
                std::string configPath = json["config_path"];
                
                startTask(taskId, configPath);
                
                json response = {
                    {"code", 0},
                    {"msg", "Task started successfully"},
                    {"task_id", taskId}
                };
                res.set_content(response.dump(), "application/json");
                
            } catch (const std::exception& e) {
                json response = {
                    {"code", -1},
                    {"msg", std::string("Error: ") + e.what()}
                };
                res.set_content(response.dump(), "application/json");
            }
        });
        
        // POST /task/stop - 停止任务
        server.Post("/task/stop", [this](const httplib::Request& req, httplib::Response& res) {
            try {
                auto json = nlohmann::json::parse(req.body);
                int taskId = json["task_id"];
                
                stopTask(taskId);
                
                json response = {
                    {"code", 0},
                    {"msg", "Task stopped successfully"},
                    {"task_id", taskId}
                };
                res.set_content(response.dump(), "application/json");
                
            } catch (const std::exception& e) {
                json response = {
                    {"code", -1},
                    {"msg", std::string("Error: ") + e.what()}
                };
                res.set_content(response.dump(), "application/json");
            }
        });
        
        // GET /task/status - 查询任务状态
        server.Get("/task/status", [this](const httplib::Request& req, httplib::Response& res) {
            try {
                int taskId = std::stoi(req.get_param_value("task_id"));
                
                std::lock_guard<std::mutex> lock(mtx);
                if (taskStatus.find(taskId) == taskStatus.end()) {
                    throw std::runtime_error("Task not found");
                }
                
                json response = {
                    {"code", 0},
                    {"data", {
                        {"task_id", taskId},
                        {"status", taskStatus[taskId]},
                        {"config_path", taskConfigs[taskId] ? "loaded" : "none"}
                    }}
                };
                res.set_content(response.dump(), "application/json");
                
            } catch (const std::exception& e) {
                json response = {
                    {"code", -1},
                    {"msg", std::string("Error: ") + e.what()}
                };
                res.set_content(response.dump(), "application/json");
            }
        });
        
        // GET /task/list - 任务列表
        server.Get("/task/list", [this](const httplib::Request& req, httplib::Response& res) {
            std::lock_guard<std::mutex> lock(mtx);
            
            json taskList = json::array();
            for (const auto& [taskId, status] : taskStatus) {
                taskList.push_back({
                    {"task_id", taskId},
                    {"status", status}
                });
            }
            
            json response = {
                {"code", 0},
                {"data", taskList}
            };
            res.set_content(response.dump(), "application/json");
        });
        
        // POST /config/generate - 生成配置文件
        server.Post("/config/generate", [this](const httplib::Request& req, httplib::Response& res) {
            try {
                auto json = nlohmann::json::parse(req.body);
                std::string configPath = generateConfig(json);
                
                json response = {
                    {"code", 0},
                    {"msg", "Config generated successfully"},
                    {"config_path", configPath}
                };
                res.set_content(response.dump(), "application/json");
                
            } catch (const std::exception& e) {
                json response = {
                    {"code", -1},
                    {"msg", std::string("Error: ") + e.what()}
                };
                res.set_content(response.dump(), "application/json");
            }
        });
    }
    
    void startTask(int taskId, const std::string& configPath) {
        std::lock_guard<std::mutex> lock(mtx);
        
        // 检查任务是否已在运行
        if (taskThreads.find(taskId) != taskThreads.end()) {
            throw std::runtime_error("Task already running");
        }
        
        // 解析配置
        auto config = std::make_shared<Config>();
        ConfigParser parser;
        if (!parser.parse(configPath, *config)) {
            throw std::runtime_error("Failed to parse config file");
        }
        
        // 创建线程运行TASK
        auto thread = std::make_unique<std::thread>([this, taskId, config]() {
            try {
                taskStatus[taskId] = "running";
                
                Detech task(*config);
                task.init();
                task.run();  // 阻塞运行
                
                taskStatus[taskId] = "stopped";
            } catch (const std::exception& e) {
                LOG(ERROR) << "[Task-" << taskId << "] Error: " << e.what();
                taskStatus[taskId] = "error";
            }
        });
        
        taskThreads[taskId] = std::move(thread);
        taskConfigs[taskId] = config;
        
        LOG(INFO) << "[TaskManager] Task " << taskId << " started";
    }
    
    void stopTask(int taskId) {
        std::lock_guard<std::mutex> lock(mtx);
        
        if (taskThreads.find(taskId) == taskThreads.end()) {
            throw std::runtime_error("Task not found");
        }
        
        // ⚠️ 这里需要实现优雅停止机制
        // 可以使用 std::atomic<bool> 标志位
        // 或者在Detech中添加stop()方法
        
        if (taskThreads[taskId]->joinable()) {
            taskThreads[taskId]->join();
        }
        
        taskThreads.erase(taskId);
        taskConfigs.erase(taskId);
        taskStatus[taskId] = "stopped";
        
        LOG(INFO) << "[TaskManager] Task " << taskId << " stopped";
    }
    
    std::string generateConfig(const json& config) {
        int taskId = config["task_id"];
        std::string configPath = "config/task" + std::to_string(taskId) + ".ini";
        
        std::ofstream file(configPath);
        if (!file.is_open()) {
            throw std::runtime_error("Cannot create config file");
        }
        
        // [task]
        file << "[task]\n";
        file << "task_id=" << taskId << "\n";
        file << "task_name=" << config.value("task_name", "task_" + std::to_string(taskId)) << "\n\n";
        
        // [video]
        file << "[video]\n";
        file << "source=" << config["video"]["source"].get<std::string>() << "\n";
        file << "width=" << config["video"].value("width", 1920) << "\n";
        file << "height=" << config["video"].value("height", 1080) << "\n";
        file << "fps=" << config["video"].value("fps", 25) << "\n";
        file << "enable=true\n\n";
        
        // [model]
        file << "[model]\n";
        file << "model_path=" << config["model"]["model_path"].get<std::string>() << "\n";
        file << "confidence_threshold=" << config["model"].value("confidence_threshold", 0.5) << "\n";
        file << "nms_threshold=" << config["model"].value("nms_threshold", 0.45) << "\n";
        file << "threads=" << config["model"].value("threads", 1) << "\n\n";
        
        // [rtmp]
        file << "[rtmp]\n";
        file << "enable=true\n";
        file << "rtmp_url=" << config["rtmp"]["rtmp_url"].get<std::string>() << "\n";
        file << "fps=" << config["rtmp"].value("fps", 25) << "\n\n";
        
        // [alarm]
        file << "[alarm]\n";
        file << "enable=" << (config["alarm"].value("enable", true) ? "true" : "false") << "\n";
        file << "hook_url=" << config["alarm"]["hook_url"].get<std::string>() << "\n";
        file << "confidence_threshold=" << config["alarm"].value("confidence_threshold", 0.6) << "\n";
        file << "cooldown_time=" << config["alarm"].value("cooldown_time", 10) << "\n\n";
        
        // [regions]
        if (config.contains("regions")) {
            file << "[regions]\n";
            for (const auto& region : config["regions"]) {
                std::string regionId = region["region_id"];
                file << regionId << "=";
                
                // 转换为 [[x1,y1],[x2,y2],...] 格式
                file << "[";
                for (size_t i = 0; i < region["polygon"].size(); ++i) {
                    file << "[" << region["polygon"][i][0] << "," << region["polygon"][i][1] << "]";
                    if (i < region["polygon"].size() - 1) file << ",";
                }
                file << "]\n";
            }
            file << "\n";
        }
        
        // [features]
        file << "[features]\n";
        file << "enable_rtmp=true\n";
        file << "enable_draw=true\n";
        file << "enable_alarm=true\n";
        
        file.close();
        
        LOG(INFO) << "[TaskManager] Config generated: " << configPath;
        return configPath;
    }
    
    void run() {
        LOG(INFO) << "[TaskManager] Server listening on port 7000";
        server.listen("0.0.0.0", 7000);
    }
};

int main(int argc, char* argv[]) {
    google::InitGoogleLogging(argv[0]);
    FLAGS_logtostderr = true;
    FLAGS_colorlogtostderr = true;
    
    TaskManager manager(7000);
    manager.run();
    
    return 0;
}
```

**CMakeLists.txt配置**:
```cmake
# 添加TaskManager可执行文件
add_executable(TaskManager
    src/TaskManager.cpp
    src/Detech.cpp
    src/Yolov11Engine.cpp
    src/RTMPEncoder.cpp
    src/AlarmCallback.cpp
    src/ConfigParser.cpp
)

target_link_libraries(TaskManager
    cpp-httplib
    nlohmann_json::nlohmann_json
    glog::glog
    opencv_world
    onnxruntime
    ${FFMPEG_LIBRARIES}
)
```

**编译**:
```bash
cd TASK
cmake --build build --config Release
```

**测试**:
```bash
# 1. 启动TaskManager
.\build\Release\TaskManager.exe

# 2. 生成配置文件
curl -X POST http://localhost:7000/config/generate \
  -H "Content-Type: application/json" \
  -d @config_template.json

# 3. 启动任务
curl -X POST http://localhost:7000/task/start \
  -H "Content-Type: application/json" \
  -d '{"task_id": 1, "config_path": "config/task1.ini"}'

# 4. 查询状态
curl http://localhost:7000/task/status?task_id=1

# 5. 任务列表
curl http://localhost:7000/task/list

# 6. 停止任务
curl -X POST http://localhost:7000/task/stop \
  -H "Content-Type: application/json" \
  -d '{"task_id": 1}'
```

**配置模板**（`config_template.json`）:
```json
{
  "task_id": 1,
  "task_name": "entrance_monitor",
  "video": {
    "source": "rtsp://admin:Admin123@192.168.1.100:554/stream1",
    "width": 1920,
    "height": 1080,
    "fps": 25
  },
  "model": {
    "model_path": "models/SafeHat.onnx",
    "confidence_threshold": 0.5,
    "nms_threshold": 0.45,
    "threads": 1
  },
  "rtmp": {
    "rtmp_url": "rtmp://localhost:1935/live/camera_1",
    "fps": 25
  },
  "alarm": {
    "enable": true,
    "hook_url": "http://localhost:48080/admin-api/device/alarm",
    "confidence_threshold": 0.6,
    "cooldown_time": 10
  },
  "regions": [
    {
      "region_id": "entrance",
      "polygon": [[100,100], [500,100], [500,400], [100,400]]
    }
  ]
}
```

---

### ⭐ 阶段2.5：ByteTrack目标追踪集成（3-4天）重要增强

#### 为什么需要ByteTrack？

**当前问题**：
- ❌ 每一帧独立检测，无法知道"这是第几个人"
- ❌ 无法跨帧追踪目标（上一帧的人A和这一帧的人B是不是同一个人）
- ❌ 无法统计进入/离开人数
- ❌ 无法分析轨迹
- ❌ 无法检测区域停留时长
- ❌ 无法检测徘徊行为

**加入ByteTrack后**：
- ✅ 每个目标有唯一ID（1号人、2号人、3号人...）
- ✅ 跨帧追踪（知道这个人从哪来、要去哪）
- ✅ 人数统计（进入5人、离开2人、当前3人）
- ✅ 轨迹分析（画出移动路径）
- ✅ 停留时长告警（"3号人在禁区停留了5分钟"）
- ✅ 徘徊检测（"5号人在入口徘徊了10分钟"）

#### Day 4：下载并集成ByteTrack C++代码

**步骤1：从GitHub获取ByteTrack源码**

ByteTrack官方仓库：https://github.com/ifzhang/ByteTrack

```bash
# 方法1：克隆整个仓库（推荐）
git clone https://github.com/ifzhang/ByteTrack.git
cd ByteTrack

# 方法2：只下载C++部分（如果只需要C++代码）
# 访问 https://github.com/ifzhang/ByteTrack/tree/main/deploy/cpp
# 手动下载以下文件到 TASK/src/bytetrack/ 目录：
```

**步骤2：需要的文件列表**

从 `ByteTrack/deploy/cpp/include/` 复制以下文件到 `TASK/src/bytetrack/`：

```
TASK/src/bytetrack/
├── BYTETracker.h
├── BYTETracker.cpp
├── STrack.h
├── STrack.cpp
├── KalmanFilter.h
├── KalmanFilter.cpp
├── lapjv.h
├── lapjv.cpp
└── dataType.h
```

**步骤3：修改CMakeLists.txt**

```cmake
# TASK/CMakeLists.txt

# 添加ByteTrack源文件
set(BYTETRACK_SOURCES
    src/bytetrack/BYTETracker.cpp
    src/bytetrack/STrack.cpp
    src/bytetrack/KalmanFilter.cpp
    src/bytetrack/lapjv.cpp
)

# 修改TASK可执行文件
add_executable(TASK
    src/main.cpp
    src/Detech.cpp
    src/Yolov11Engine.cpp
    src/RTMPEncoder.cpp
    src/AlarmCallback.cpp
    src/ConfigParser.cpp
    ${BYTETRACK_SOURCES}  # ⭐ 新增
)

# 添加包含目录
target_include_directories(TASK PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/src
    ${CMAKE_CURRENT_SOURCE_DIR}/src/bytetrack  # ⭐ 新增
)
```

**步骤4：更新Config结构体**

```cpp
// TASK/src/Config.h

typedef struct Config {
    // ... 现有字段 ...
    
    // ⭐ 新增：追踪配置
    bool enableTracking;                // 是否启用追踪
    int trackMaxLostFrames;             // 最多丢失帧数（默认30）
    int trackBuffer;                    // track缓冲帧数（默认30）
    float trackMatchThreshold;          // 匹配阈值（默认0.8）
    int trackMinBoxArea;                // 最小框面积（默认100）
    
    // ⭐ 新增：高级告警配置
    bool enableRegionStayAlarm;         // 区域停留告警
    int regionStayThreshold;            // 停留时长阈值（秒）
    bool enableLoiteringAlarm;          // 徘徊告警
    int loiteringThreshold;             // 徘徊时长阈值（秒）
    
    // 现有字段...
    std::string rtspUrl;
    std::string rtmpUrl;
    // ...
} Config;
```

#### Day 5-6：实现追踪逻辑

**新建文件**: `TASK/src/Detech.h` （添加追踪相关成员）

```cpp
// TASK/src/Detech.h

#include "bytetrack/BYTETracker.h"  // ⭐ 新增

class Detech {
private:
    // ... 现有成员 ...
    
    // ⭐ 新增：ByteTrack追踪器
    BYTETracker* _tracker;
    
    // ⭐ 新增：追踪状态管理
    std::map<int, std::chrono::steady_clock::time_point> _trackEnterTime;   // track进入区域时间
    std::map<int, std::vector<cv::Point>> _trackTrajectory;                 // track轨迹
    std::map<int, std::chrono::steady_clock::time_point> _trackFirstSeen;   // track首次出现时间
    
    // ⭐ 新增：追踪相关函数
    std::vector<STrack> updateTracking(const std::vector<Detection>& detections);
    bool checkRegionStayAlarm(int trackId, const cv::Point& center);
    bool checkLoiteringAlarm(int trackId);
    void drawTrackingInfo(cv::Mat& frame, const std::vector<STrack>& tracks);
};
```

**修改文件**: `TASK/src/Detech.cpp` （实现追踪逻辑）

```cpp
// TASK/src/Detech.cpp

bool Detech::init() {
    // ... 现有初始化代码 ...
    
    // ⭐ 新增：初始化ByteTrack追踪器
    if (_config.enableTracking) {
        _tracker = new BYTETracker(
            25,  // fps
            _config.trackBuffer,
            _config.trackMatchThreshold,
            _config.trackMinBoxArea
        );
        LOG(INFO) << "[ByteTrack] Tracker initialized";
    }
    
    return true;
}

void Detech::run() {
    // ... 视频解码循环 ...
    
    while (true) {
        // 1. 读取帧
        AVPacket* packet = av_packet_alloc();
        if (av_read_frame(_videoCtx, packet) < 0) {
            break;
        }
        
        // 2. 解码
        cv::Mat frame = decodeFrame(packet);
        
        // 3. YOLO检测
        std::vector<Detection> detections = _yoloEngine->detect(frame);
        
        // ⭐ 4. ByteTrack追踪（新增）
        std::vector<STrack> tracks;
        if (_config.enableTracking) {
            tracks = updateTracking(detections);
        }
        
        // 5. 区域过滤 + 告警
        std::vector<STrack> alarmTracks;
        for (const auto& track : tracks) {
            // 检查是否在区域内
            cv::Point center(track.tlwh[0] + track.tlwh[2]/2, 
                           track.tlwh[1] + track.tlwh[3]/2);
            
            bool inRegion = false;
            for (const auto& region : _config.regions["default"]) {
                if (cv::pointPolygonTest(region, center, false) >= 0) {
                    inRegion = true;
                    
                    // ⭐ 高级告警：区域停留时长检测
                    if (_config.enableRegionStayAlarm) {
                        if (checkRegionStayAlarm(track.track_id, center)) {
                            LOG(WARNING) << "[ALARM] Track " << track.track_id 
                                       << " stayed in region for too long!";
                            // 触发告警
                        }
                    }
                    
                    break;
                }
            }
            
            if (inRegion && track.score >= _config.alarmConfidenceThreshold) {
                alarmTracks.push_back(track);
            }
        }
        
        // ⭐ 6. 徘徊检测
        if (_config.enableLoiteringAlarm) {
            for (const auto& track : tracks) {
                if (checkLoiteringAlarm(track.track_id)) {
                    LOG(WARNING) << "[ALARM] Track " << track.track_id 
                               << " is loitering!";
                    // 触发告警
                }
            }
        }
        
        // 7. 发送告警（带track_id）
        if (!alarmTracks.empty()) {
            auto alarmNow = std::chrono::steady_clock::now();
            auto elapsed = std::chrono::duration_cast<std::chrono::seconds>(
                alarmNow - _lastAlarmTime).count();
            
            if (elapsed >= _config.alarmCooldownTime) {
                _lastAlarmTime = alarmNow;
                
                // 转换为Detection格式发送
                std::vector<Detection> alarmDetections;
                for (const auto& track : alarmTracks) {
                    Detection det;
                    det.x = track.tlwh[0];
                    det.y = track.tlwh[1];
                    det.w = track.tlwh[2];
                    det.h = track.tlwh[3];
                    det.confidence = track.score;
                    det.classId = 0;  // 假设是person类
                    det.track_id = track.track_id;  // ⭐ 新增track_id
                    alarmDetections.push_back(det);
                }
                
                // 异步发送告警
                std::thread([this, alarmDetections]() {
                    _alarmCallback->sendAlarm(
                        _config.taskId,
                        alarmDetections,
                        "region_main",
                        getCurrentTimestamp()
                    );
                }).detach();
            }
        }
        
        // 8. 绘制追踪信息
        if (_config.enableDrawRtmp && _config.enableTracking) {
            drawTrackingInfo(frame, tracks);
        }
        
        // 9. RTMP推流
        if (_config.enableRtmp) {
            _rtmpEncoder->encodeAndPush(frame);
        }
    }
}

// ⭐ 新增：更新追踪
std::vector<STrack> Detech::updateTracking(const std::vector<Detection>& detections) {
    // 转换为ByteTrack需要的格式
    std::vector<Object> objects;
    for (const auto& det : detections) {
        Object obj;
        obj.rect.x = det.x;
        obj.rect.y = det.y;
        obj.rect.width = det.w;
        obj.rect.height = det.h;
        obj.prob = det.confidence;
        obj.label = det.classId;
        objects.push_back(obj);
    }
    
    // 调用ByteTrack更新
    std::vector<STrack> tracks = _tracker->update(objects);
    
    // 记录首次出现时间
    for (const auto& track : tracks) {
        if (_trackFirstSeen.find(track.track_id) == _trackFirstSeen.end()) {
            _trackFirstSeen[track.track_id] = std::chrono::steady_clock::now();
            LOG(INFO) << "[Track] New track ID: " << track.track_id;
        }
        
        // 记录轨迹
        cv::Point center(track.tlwh[0] + track.tlwh[2]/2, 
                        track.tlwh[1] + track.tlwh[3]/2);
        _trackTrajectory[track.track_id].push_back(center);
        
        // 限制轨迹长度（保留最近100个点）
        if (_trackTrajectory[track.track_id].size() > 100) {
            _trackTrajectory[track.track_id].erase(
                _trackTrajectory[track.track_id].begin());
        }
    }
    
    return tracks;
}

// ⭐ 新增：检查区域停留时长
bool Detech::checkRegionStayAlarm(int trackId, const cv::Point& center) {
    auto now = std::chrono::steady_clock::now();
    
    // 如果是第一次进入区域
    if (_trackEnterTime.find(trackId) == _trackEnterTime.end()) {
        _trackEnterTime[trackId] = now;
        return false;
    }
    
    // 计算停留时长
    auto elapsed = std::chrono::duration_cast<std::chrono::seconds>(
        now - _trackEnterTime[trackId]).count();
    
    // 超过阈值触发告警
    if (elapsed >= _config.regionStayThreshold) {
        _trackEnterTime.erase(trackId);  // 重置，避免重复告警
        return true;
    }
    
    return false;
}

// ⭐ 新增：检查徘徊行为
bool Detech::checkLoiteringAlarm(int trackId) {
    // 需要至少有10个轨迹点
    if (_trackTrajectory[trackId].size() < 10) {
        return false;
    }
    
    // 计算轨迹的位移
    const auto& trajectory = _trackTrajectory[trackId];
    cv::Point start = trajectory.front();
    cv::Point end = trajectory.back();
    float displacement = cv::norm(end - start);
    
    // 如果位移很小，但存在时间很长，判断为徘徊
    auto firstSeen = _trackFirstSeen[trackId];
    auto now = std::chrono::steady_clock::now();
    auto duration = std::chrono::duration_cast<std::chrono::seconds>(
        now - firstSeen).count();
    
    // 徘徊判断：存在超过阈值时间，但位移很小（< 50像素）
    if (duration >= _config.loiteringThreshold && displacement < 50) {
        return true;
    }
    
    return false;
}

// ⭐ 新增：绘制追踪信息
void Detech::drawTrackingInfo(cv::Mat& frame, const std::vector<STrack>& tracks) {
    for (const auto& track : tracks) {
        // 绘制检测框
        cv::Rect rect(track.tlwh[0], track.tlwh[1], track.tlwh[2], track.tlwh[3]);
        cv::rectangle(frame, rect, cv::Scalar(0, 255, 0), 2);
        
        // 绘制track_id
        std::string label = "ID:" + std::to_string(track.track_id);
        int baseLine;
        cv::Size labelSize = cv::getTextSize(label, cv::FONT_HERSHEY_SIMPLEX, 
                                            0.5, 1, &baseLine);
        cv::rectangle(frame, 
                     cv::Point(rect.x, rect.y - labelSize.height - 5),
                     cv::Point(rect.x + labelSize.width, rect.y),
                     cv::Scalar(0, 255, 0), -1);
        cv::putText(frame, label, 
                   cv::Point(rect.x, rect.y - 5),
                   cv::FONT_HERSHEY_SIMPLEX, 0.5, 
                   cv::Scalar(0, 0, 0), 1);
        
        // 绘制轨迹（可选）
        if (_trackTrajectory.find(track.track_id) != _trackTrajectory.end()) {
            const auto& trajectory = _trackTrajectory[track.track_id];
            for (size_t i = 1; i < trajectory.size(); ++i) {
                cv::line(frame, trajectory[i-1], trajectory[i], 
                        cv::Scalar(255, 0, 255), 2);
            }
        }
    }
}

// ⭐ 新增：资源释放
void Detech::releaseResources() {
    // ... 现有释放代码 ...
    
    // 释放ByteTrack追踪器
    if (_tracker) {
        delete _tracker;
        _tracker = nullptr;
    }
    
    LOG(INFO) << "[CLEANUP] All resources released successfully";
}
```

#### Day 7：配置文件和测试

**更新配置文件** (`TASK/config/test.ini`)：

```ini
[task]
task_id=1
task_name=entrance_monitor

[video]
source=rtsp://admin:Admin123@192.168.1.100:554/stream1
width=1920
height=1080
fps=25

[model]
model_path=models/SafeHat.onnx
confidence_threshold=0.5

[rtmp]
enable=true
rtmp_url=rtmp://localhost:1935/live/camera_1
fps=25

[alarm]
enable=true
hook_url=http://localhost:48080/admin-api/device/alarm
confidence_threshold=0.6
cooldown_time=10

[regions]
entrance=[[100,200],[300,200],[300,400],[100,400]]

# ⭐ 新增：追踪配置
[tracking]
enable=true
max_lost_frames=30        # 最多丢失30帧后删除track
track_buffer=30           # track缓冲帧数
match_threshold=0.8       # 匹配阈值
min_box_area=100          # 最小框面积（像素）

# ⭐ 新增：高级告警配置
[advanced_alarm]
enable_region_stay=true
region_stay_threshold=60  # 在区域内停留超过60秒告警

enable_loitering=true
loitering_threshold=120   # 徘徊超过120秒告警

[features]
enable_rtmp=true
enable_draw=true
enable_alarm=true
enable_tracking=true      # ⭐ 新增
```

**测试验证**：

```bash
# 1. 编译
cd TASK
cmake --build build --config Release

# 2. 运行TASK（带追踪）
.\build\Release\TASK.exe config\test.ini

# 3. 观察日志
[ByteTrack] Tracker initialized
[Track] New track ID: 1
[Track] New track ID: 2
[Track] New track ID: 3

# 4. 播放视频（应该能看到ID标签）
ffplay http://localhost/live/camera_1.flv

# 5. 验证告警（带track_id）
curl http://localhost:48080/admin-api/device/alarm
# 应该能看到：
# {
#   "detections": [
#     {"track_id": 3, "class": "person", "confidence": 0.85}
#   ]
# }
```

**验收标准**：

- [ ] 视频上显示track_id（1、2、3...）
- [ ] track_id连续稳定（不频繁跳变）
- [ ] 告警数据包含track_id
- [ ] 区域停留告警正常触发
- [ ] 徘徊检测正常触发
- [ ] 性能无明显下降（FPS仍≥20）

---

### 阶段3：前端集成TaskManager（2-3天）

#### Day 4-5：前端API对接

**新建文件**: `WEB/src/api/task/index.ts`

```typescript
import {defHttp} from '@/utils/http/axios';

// ====================== 接口类型定义 ======================

export interface TaskConfig {
  task_id: number;
  task_name?: string;
  video: {
    source: string;       // RTSP地址
    width: number;
    height: number;
    fps: number;
  };
  model: {
    model_path: string;
    confidence_threshold: number;
    nms_threshold?: number;
    threads?: number;
  };
  rtmp: {
    rtmp_url: string;
    fps: number;
  };
  alarm: {
    enable: boolean;
    hook_url: string;
    confidence_threshold: number;
    cooldown_time: number;
  };
  regions?: Array<{
    region_id: string;
    polygon: number[][];
  }>;
}

export interface TaskStatus {
  task_id: number;
  status: 'running' | 'stopped' | 'error';
  config_path?: string;
}

// ====================== API函数 ======================

// 生成配置文件
export const generateConfig = (config: TaskConfig) => {
  return defHttp.post<{config_path: string}>({
    url: '/dev-api/task/config/generate',
    data: config
  });
};

// 启动任务
export const startTask = (taskId: number, configPath: string) => {
  return defHttp.post({
    url: '/dev-api/task/start',
    data: { task_id: taskId, config_path: configPath }
  });
};

// 停止任务
export const stopTask = (taskId: number) => {
  return defHttp.post({
    url: '/dev-api/task/stop',
    data: { task_id: taskId }
  });
};

// 查询任务状态
export const getTaskStatus = (taskId: number) => {
  return defHttp.get<TaskStatus>({
    url: '/dev-api/task/status',
    params: { task_id: taskId }
  });
};

// 获取所有任务列表
export const getTaskList = () => {
  return defHttp.get<TaskStatus[]>({
    url: '/dev-api/task/list'
  });
};
```

**配置前端代理**（`WEB/.env.development`）:
```javascript
VITE_PORT = 8099
VITE_PROXY = [
  ["/api/v1/buckets", "http://localhost:9000/api/v1/buckets"],
  ["/dev-api/nodeRed", "http://127.0.0.1:1880"],
  ["/dev-api/task", "http://127.0.0.1:7000"],              // ⚠️ 新增
  ["/dev-api", "http://127.0.0.1:48080/admin-api"]
]
```

#### Day 6：扩展摄像头管理页面

**文件**: `WEB/src/views/camera/index.vue`

**在现有页面添加AI任务按钮**:
```vue
<template>
  <div class="camera-container">
    <BasicTable @register="registerTable">
      <template #bodyCell="{ column, record }">
        <template v-if="column.dataIndex === 'action'">
          <TableAction :actions="getTableActions(record)" />
        </template>
      </template>
    </BasicTable>
  </div>
</template>

<script lang="ts" setup>
import { useMessage } from '@/hooks/web/useMessage';
import { startTask, stopTask, generateConfig } from '@/api/task';

const { createMessage } = useMessage();

// 扩展操作按钮
const getTableActions = (record) => {
  return [
    {
      icon: 'octicon:play-16',
      tooltip: '播放视频',
      onClick: () => handlePlay(record)
    },
    {
      icon: 'ant-design:play-circle-outlined',
      tooltip: '启动RTSP转发（无AI）',
      onClick: () => handleStartForwarding(record)
    },
    {
      icon: 'ant-design:robot-outlined',      // ⚠️ 新增
      tooltip: '启动AI检测',
      onClick: () => handleStartAI(record),
      color: 'success'
    },
    {
      icon: 'ant-design:stop-outlined',        // ⚠️ 新增
      tooltip: '停止AI检测',
      onClick: () => handleStopAI(record),
      color: 'error'
    },
    {
      icon: 'material-symbols:edit-outline-rounded',
      tooltip: '编辑',
      onClick: () => handleEdit(record)
    },
    {
      icon: 'material-symbols:delete-outline-rounded',
      tooltip: '删除',
      popConfirm: {
        title: '确定删除此设备？',
        confirm: () => handleDelete(record.id)
      }
    }
  ];
};

// ⚠️ 新增：启动AI检测任务
const handleStartAI = async (record) => {
  try {
    createMessage.loading({ content: '正在启动AI检测...', key: 'ai' });
    
    // 1. 构建配置
    const config = {
      task_id: parseInt(record.id),
      task_name: record.name,
      video: {
        source: record.source,  // RTSP地址
        width: 1920,
        height: 1080,
        fps: 25
      },
      model: {
        model_path: 'models/SafeHat.onnx',    // ⚠️ 可以让用户选择
        confidence_threshold: 0.5
      },
      rtmp: {
        rtmp_url: `rtmp://localhost:1935/live/ai_camera_${record.id}`,
        fps: 25
      },
      alarm: {
        enable: true,
        hook_url: `http://localhost:48080/admin-api/device/alarm`,
        confidence_threshold: 0.6,
        cooldown_time: 10
      }
    };
    
    // 2. 生成配置文件
    const configRes = await generateConfig(config);
    
    // 3. 启动TASK
    await startTask(parseInt(record.id), configRes.data.config_path);
    
    createMessage.success({ content: 'AI检测已启动', key: 'ai' });
    
    // 4. 更新播放地址（切换为AI流）
    record.http_stream = `http://localhost/live/ai_camera_${record.id}.flv`;
    reload();
    
  } catch (error) {
    console.error('启动AI检测失败', error);
    createMessage.error({ content: '启动AI检测失败', key: 'ai' });
  }
};

// ⚠️ 新增：停止AI检测任务
const handleStopAI = async (record) => {
  try {
    createMessage.loading({ content: '正在停止AI检测...', key: 'ai' });
    await stopTask(parseInt(record.id));
    createMessage.success({ content: 'AI检测已停止', key: 'ai' });
    reload();
  } catch (error) {
    console.error('停止AI检测失败', error);
    createMessage.error({ content: '停止AI检测失败', key: 'ai' });
  }
};
</script>
```

---

### 阶段4：视频墙展示（1-2天）

#### Day 7：新建视频墙页面

**新建文件**: `WEB/src/views/ai-task/video-wall.vue`

```vue
<template>
  <div class="video-wall-container">
    <!-- 顶部工具栏 -->
    <div class="video-wall-header">
      <h2>AI检测视频墙（{{ runningTasks.length }}/50路）</h2>
      <a-radio-group v-model:value="gridLayout" button-style="solid">
        <a-radio-button value="2x2">2x2</a-radio-button>
        <a-radio-button value="3x3">3x3</a-radio-button>
        <a-radio-button value="4x4">4x4</a-radio-button>
        <a-radio-button value="5x5">5x5</a-radio-button>
      </a-radio-group>
      <a-button @click="refreshTasks" type="primary">
        <template #icon><SyncOutlined /></template>
        刷新
      </a-button>
    </div>
    
    <!-- 视频网格 -->
    <div :class="`video-wall-grid grid-${gridLayout}`">
      <div 
        v-for="task in runningTasks" 
        :key="task.task_id"
        class="video-wall-item"
        :class="{ 'has-alarm': task.has_alarm }"
      >
        <!-- Jessibuca播放器 -->
        <Jessibuca 
          :playUrl="task.http_flv_url" 
          :hasAudio="false"
          class="video-player"
        />
        
        <!-- 信息覆盖层 -->
        <div class="video-info-overlay">
          <div class="video-title">{{ task.camera_name }}</div>
          <div class="video-stats">
            <span class="status" :class="task.status">{{ task.status }}</span>
          </div>
        </div>
        
        <!-- 告警标识 -->
        <div v-if="task.has_alarm" class="alarm-indicator">
          <WarningOutlined /> 告警中
        </div>
      </div>
    </div>
  </div>
</template>

<script lang="ts" setup>
import { ref, onMounted, onUnmounted } from 'vue';
import { getTaskList } from '@/api/task';
import Jessibuca from '@/components/Player/module/jessibuca.vue';
import { WarningOutlined, SyncOutlined } from '@ant-design/icons-vue';

const gridLayout = ref('3x3');
const runningTasks = ref([]);
const updateTimer = ref(null);

// 获取运行中的任务
const fetchRunningTasks = async () => {
  try {
    const response = await getTaskList();
    runningTasks.value = response.data
      .filter(task => task.status === 'running')
      .map(task => ({
        ...task,
        http_flv_url: `http://localhost/live/ai_task_${task.task_id}.flv`,
        camera_name: `摄像头 ${task.task_id}`,
        has_alarm: false  // ⚠️ 后续通过WebSocket更新
      }));
  } catch (error) {
    console.error('获取任务列表失败', error);
  }
};

const refreshTasks = () => {
  fetchRunningTasks();
};

onMounted(() => {
  fetchRunningTasks();
  // 每5秒更新一次状态
  updateTimer.value = setInterval(fetchRunningTasks, 5000);
});

onUnmounted(() => {
  if (updateTimer.value) {
    clearInterval(updateTimer.value);
  }
});
</script>

<style scoped lang="less">
.video-wall-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #000;
}

.video-wall-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  background: #1a1a1a;
  color: #fff;
  
  h2 {
    margin: 0;
    color: #fff;
  }
}

.video-wall-grid {
  flex: 1;
  display: grid;
  gap: 8px;
  padding: 8px;
  
  &.grid-2x2 { grid-template-columns: repeat(2, 1fr); }
  &.grid-3x3 { grid-template-columns: repeat(3, 1fr); }
  &.grid-4x4 { grid-template-columns: repeat(4, 1fr); }
  &.grid-5x5 { grid-template-columns: repeat(5, 1fr); }
}

.video-wall-item {
  position: relative;
  background: #222;
  border-radius: 4px;
  overflow: hidden;
  aspect-ratio: 16 / 9;
  
  &.has-alarm {
    box-shadow: 0 0 20px rgba(255, 77, 79, 0.8);
    border: 2px solid #ff4d4f;
  }
  
  &:hover .video-info-overlay {
    opacity: 1;
  }
}

.video-player {
  width: 100%;
  height: 100%;
}

.video-info-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
  padding: 8px;
  opacity: 0.7;
  transition: opacity 0.3s;
  
  .video-title {
    color: #fff;
    font-size: 14px;
    font-weight: bold;
  }
  
  .video-stats {
    margin-top: 4px;
    
    .status {
      font-size: 12px;
      padding: 2px 8px;
      border-radius: 3px;
      
      &.running {
        background: #52c41a;
        color: #fff;
      }
      
      &.stopped {
        background: #d9d9d9;
        color: #000;
      }
      
      &.error {
        background: #ff4d4f;
        color: #fff;
      }
    }
  }
}

.alarm-indicator {
  position: absolute;
  top: 8px;
  right: 8px;
  background: #ff4d4f;
  color: #fff;
  padding: 4px 12px;
  border-radius: 4px;
  font-size: 12px;
  font-weight: bold;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0.3; }
}
</style>
```

**添加路由**（`WEB/src/router/routes/modules/ai-task.ts`）:
```typescript
import type { RouteRecordRaw } from 'vue-router';

const routes: RouteRecordRaw[] = [
  {
    path: '/ai-task',
    name: 'AITask',
    redirect: '/ai-task/video-wall',
    meta: {
      title: 'AI检测任务',
      icon: 'ant-design:robot-outlined',
    },
    children: [
      {
        path: 'video-wall',
        name: 'VideoWall',
        component: () => import('@/views/ai-task/video-wall.vue'),
        meta: {
          title: '视频墙',
          icon: 'ant-design:appstore-outlined',
        },
      },
    ],
  },
];

export default routes;
```

---

## 🎯 第五部分：50路摄像头部署方案

### 5.1 硬件要求

| 组件 | 推荐配置 | 最低配置 | 说明 |
|------|---------|---------|------|
| **CPU** | Intel i7-12700 (12核24线程) | Intel i5-11400 (6核12线程) | 50路AI推理需要强CPU |
| **内存** | 64GB DDR4 | 32GB DDR4 | 每路TASK约500MB，50路需25GB+ |
| **存储** | 1TB NVMe SSD | 500GB SATA SSD | 模型、配置、日志 |
| **网络** | 双千兆网卡 | 单千兆网卡 | 50路视频流带宽需求大 |
| **GPU（可选）** | RTX 4070 (12GB) | RTX 3060 (12GB) | 加速AI推理（可选，CPU也能跑） |

### 5.2 软件架构

```
边缘服务器（一台物理机）
├── Windows 10/11 或 Ubuntu 22.04
├── ZLMediaKit（流媒体服务器）- 1个实例
│   ├── RTMP服务器 (:1935)
│   └── HTTP-FLV服务器 (:80)
├── TaskManager（进程管理器）- 1个实例 (:7000)
│   ├── TASK实例 #1（camera_1）
│   ├── TASK实例 #2（camera_2）
│   ├── ...
│   └── TASK实例 #50（camera_50）
├── PostgreSQL (:5432)
├── MinIO (:9000)
└── Node-RED (:1880)
```

### 5.3 资源占用估算

**单路TASK资源占用**:
```
CPU占用: ~8-10% (单核)
内存占用: ~500MB
推理速度: 20-30 FPS
RTMP推流: 1-2Mbps
```

**50路并发预估**:
```
CPU占用: ~60-80% (12核心)
内存占用: ~25-30GB
网络带宽:
  - RTSP入站: 50 × 4Mbps = 200Mbps
  - RTMP出站: 50 × 2Mbps = 100Mbps
  - 总计: ~300Mbps（需千兆网络）
```

### 5.4 部署步骤

#### Step 1: 启动ZLMediaKit

```bash
# Windows
cd F:\EASYLOT\zlmediakit
.\MediaServer.exe -c config.ini

# Linux
cd /opt/zlmediakit
./MediaServer -c config.ini
```

**验证**:
```bash
# 访问Web管理界面
http://localhost/index/api/getMediaList?secret=your_secret
```

#### Step 2: 启动TaskManager

```bash
cd F:\EASYLOT\yfeieye-main\TASK\build\Release
.\TaskManager.exe
```

**验证**:
```bash
curl http://localhost:7000/task/list
# 应该返回: {"code": 0, "data": []}
```

#### Step 3: 批量创建50路任务

**PowerShell脚本**（`create_50_tasks.ps1`）:
```powershell
$baseUrl = "http://localhost:7000"

# 摄像头IP范围: 192.168.1.100 ~ 192.168.1.149
for ($i = 1; $i -le 50; $i++) {
    $cameraIp = "192.168.1." + (100 + $i - 1)
    
    $config = @{
        task_id = $i
        task_name = "camera_$i"
        video = @{
            source = "rtsp://admin:Admin123@${cameraIp}:554/stream1"
            width = 1920
            height = 1080
            fps = 25
        }
        model = @{
            model_path = "models/SafeHat.onnx"
            confidence_threshold = 0.5
        }
        rtmp = @{
            rtmp_url = "rtmp://localhost:1935/live/camera_$i"
            fps = 25
        }
        alarm = @{
            enable = $true
            hook_url = "http://localhost:48080/admin-api/device/alarm"
            confidence_threshold = 0.6
            cooldown_time = 10
        }
    } | ConvertTo-Json -Depth 10
    
    # 1. 生成配置文件
    $configRes = Invoke-RestMethod -Uri "$baseUrl/config/generate" -Method POST -Body $config -ContentType "application/json"
    
    # 2. 启动任务
    $startBody = @{
        task_id = $i
        config_path = $configRes.config_path
    } | ConvertTo-Json
    
    Invoke-RestMethod -Uri "$baseUrl/task/start" -Method POST -Body $startBody -ContentType "application/json"
    
    Write-Host "Task $i started successfully"
    Start-Sleep -Milliseconds 500
}

Write-Host "All 50 tasks started!"
```

**执行**:
```powershell
.\create_50_tasks.ps1
```

#### Step 4: 访问前端视频墙

浏览器访问：`http://localhost:8099/ai-task/video-wall`

选择5x5布局 → 同时看到25路视频（可滚动查看全部50路）

---

## ✅ 第六部分：验收标准

### 6.1 功能验收

- [ ] **单路TASK正常运行**
  - RTSP拉流成功
  - YOLO推理正常
  - RTMP推流成功
  - HTTP-FLV播放正常
  - 告警回调成功

- [ ] **区域检测功能**
  - 配置多边形区域
  - 区域内检测 → 触发告警 ✅
  - 区域外检测 → 不触发告警 ✅

- [ ] **TaskManager功能**
  - 成功管理50个TASK实例
  - API响应时间 < 100ms
  - 启动任务成功率 > 95%
  - 任务状态查询准确

- [ ] **前端功能**
  - 启动/停止AI任务
  - 实时查看任务状态
  - 视频墙播放多路视频
  - 告警实时显示（< 1秒）

- [ ] **告警处理**
  - TASK → DEVICE告警接口正常
  - Node-RED规则引擎触发
  - 企业微信/钉钉通知成功

### 6.2 性能验收

- [ ] **单路TASK性能**
  - FPS ≥ 20
  - 视频延迟 ≤ 2秒
  - CPU占用 ≤ 15% (单核)
  - 内存占用 ≤ 600MB

- [ ] **50路并发性能**
  - 所有任务同时运行
  - 系统稳定（无崩溃）
  - 总CPU占用 < 85%
  - 总内存占用 < 30GB

- [ ] **稳定性测试**
  - 24小时连续运行
  - 内存增长 < 10%
  - 无内存泄漏
  - RTSP断线自动重连

### 6.3 用户体验验收

- [ ] 前端操作流畅（启动任务 < 3秒）
- [ ] 视频墙布局可调整（2x2 / 3x3 / 4x4 / 5x5）
- [ ] 告警实时显示（延迟 < 1秒）
- [ ] 日志可查询（Glog输出）
- [ ] 配置管理友好（Web界面，非手动编辑INI）

---

## 📚 第七部分：配置文件参考

### 7.1 ZLMediaKit配置（低延迟）

**文件**: `F:\EASYLOT\zlmediakit\config.ini`

```ini
[general]
enableVhost=0
flowThreshold=1024

[http]
port=80
sslport=443

[rtmp]
port=1935
sslport=19350

[rtsp]
port=554
sslport=322

[rtp]
port=10000

[protocol]
modify_stamp=1
enable_audio=1

# ⚠️ 低延迟配置
[rtsp.stream]
gop_cache=0

[rtmp.stream]
gop_cache=0

[hls.stream]
gop_cache=0

[http-flv.stream]
gop_cache=0
```

### 7.2 TASK配置完整示例

**文件**: `TASK/config/task1.ini`

```ini
[task]
task_id=1
task_name=entrance_monitor

[video]
source=rtsp://admin:Admin123@192.168.1.100:554/stream1
width=1920
height=1080
fps=25
enable=true

[model]
model_path=models/SafeHat.onnx
classes_path=models/SafeHat_cn.txt
confidence_threshold=0.5
nms_threshold=0.45
threads=1

[rtmp]
enable=true
rtmp_url=rtmp://localhost:1935/live/camera_1
fps=25
enable_draw=true

[alarm]
enable=true
hook_url=http://localhost:48080/admin-api/device/alarm
confidence_threshold=0.6
cooldown_time=10

[regions]
entrance=[[100,100],[500,100],[500,400],[100,400]]
parking=[[600,100],[900,100],[900,400],[600,400]]

[features]
enable_rtmp=true
enable_draw=true
enable_alarm=true
```

### 7.3 前端环境配置

**文件**: `WEB/.env`

```bash
# 端口号
VITE_PORT = 8099

# 网站标题
VITE_GLOB_APP_TITLE = 逸飞 AI 智眼管控平台

# 简称
VITE_GLOB_APP_SHORT_NAME = BasicLab_IoT_Admin

# 租户开关
VITE_GLOB_APP_TENANT_ENABLE = true

# 验证码的开关
VITE_GLOB_APP_CAPTCHA_ENABLE = true

# 文档地址的开关
VITE_APP_DOCALERT_ENABLE=true

# 百度统计
VITE_APP_BAIDU_CODE = eb21166668bf766b9d059a6fd1c10777
```

**文件**: `WEB/.env.development`

```bash
# 本地开发环境
VITE_PUBLIC_PATH = /

# 本地开发代理
VITE_PROXY = [["/api/v1/buckets","http://localhost:9000/api/v1/buckets"],["/dev-api/nodeRed","http://127.0.0.1:1880"],["/dev-api/task","http://127.0.0.1:7000"],["/dev-api","http://127.0.0.1:48080/admin-api"]]

# 是否删除Console.log
VITE_DROP_CONSOLE = false

# 基础页面标题
VITE_GLOB_APP_TITLE = yFeiEye

# 基础页面地址
VITE_GLOB_BASE_URL = "http://localhost:48080"

# 接口地址
VITE_GLOB_API_URL = /dev-api

# 文件上传接口
VITE_GLOB_UPLOAD_URL = http://localhost:48080/admin-api

# 接口地址前缀
VITE_GLOB_API_URL_PREFIX =
```

---

## 🐛 第八部分：故障排查指南

### 8.1 常见问题速查

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| RTSP连接失败 | 摄像头地址错误/网络不通 | 1. ping摄像头IP<br>2. 用VLC测试RTSP地址<br>3. 检查用户名密码 |
| RTMP推流失败 | ZLMediaKit未启动/端口占用 | 1. 检查MediaServer进程<br>2. `netstat -ano \| findstr 1935` |
| HTTP-FLV播放失败 | 流未推送/防火墙阻止 | 1. 访问 `http://localhost/index/api/getMediaList`<br>2. 关闭防火墙测试 |
| 告警未触发 | enable_alarm=false | 检查 `[features]` 和 `[alarm]` 两处配置 |
| TaskManager API失败 | 端口冲突/服务未启动 | 1. 检查端口7000是否被占用<br>2. 查看TaskManager日志 |
| 内存泄漏 | FFmpeg资源未释放 | 调用 `releaseResources()` |
| 前端跨域错误 | 代理配置错误 | 检查 `WEB/.env.development` 中的 `VITE_PROXY` |
| 中文乱码 | 文件编码非UTF-8 | 确保所有文件使用UTF-8编码 |

### 8.2 日志文件位置

```
TASK模块日志:
- 控制台输出（Glog）
- 可配置到文件: FLAGS_log_dir = "./logs"

TaskManager日志:
- 控制台输出
- 包含所有API请求和任务状态变化

ZLMediaKit日志:
- F:\EASYLOT\zlmediakit\log\

前端日志:
- 浏览器Console
- Network面板查看API请求
```

### 8.3 性能监控命令

**Windows**:
```powershell
# 查看TaskManager进程
Get-Process | Where-Object {$_.Name -like "*TaskManager*"} | Select-Object Id,ProcessName,CPU,WorkingSet

# 查看所有TASK进程
Get-Process | Where-Object {$_.Name -like "*TASK*"} | Select-Object Id,ProcessName,CPU,WorkingSet

# 查看端口占用
netstat -ano | findstr "7000"
netstat -ano | findstr "1935"
netstat -ano | findstr "80"

# 监控网络带宽
Get-Counter '\Network Interface(*)\Bytes Total/sec' -SampleInterval 1 -MaxSamples 10
```

**Linux**:
```bash
# 查看进程
ps aux | grep TaskManager
ps aux | grep TASK

# 查看端口
netstat -tlnp | grep 7000
netstat -tlnp | grep 1935

# 监控资源
htop
iotop
nload
```

---

## 📊 第九部分：API接口完整文档

### 9.1 TaskManager API

**Base URL**: `http://localhost:7000`  
**通过前端代理**: `/dev-api/task`

#### POST /task/start - 启动任务

**请求**:
```json
{
  "task_id": 1,
  "config_path": "config/task1.ini"
}
```

**响应**:
```json
{
  "code": 0,
  "msg": "Task started successfully",
  "task_id": 1
}
```

#### POST /task/stop - 停止任务

**请求**:
```json
{
  "task_id": 1
}
```

**响应**:
```json
{
  "code": 0,
  "msg": "Task stopped successfully",
  "task_id": 1
}
```

#### GET /task/status - 查询任务状态

**请求**:
```
GET /task/status?task_id=1
```

**响应**:
```json
{
  "code": 0,
  "data": {
    "task_id": 1,
    "status": "running",
    "config_path": "loaded"
  }
}
```

#### GET /task/list - 任务列表

**请求**:
```
GET /task/list
```

**响应**:
```json
{
  "code": 0,
  "data": [
    {
      "task_id": 1,
      "status": "running"
    },
    {
      "task_id": 2,
      "status": "stopped"
    }
  ]
}
```

#### POST /config/generate - 生成配置文件

**请求**:
```json
{
  "task_id": 1,
  "task_name": "entrance_monitor",
  "video": {
    "source": "rtsp://admin:Admin123@192.168.1.100:554/stream1",
    "width": 1920,
    "height": 1080,
    "fps": 25
  },
  "model": {
    "model_path": "models/SafeHat.onnx",
    "confidence_threshold": 0.5
  },
  "rtmp": {
    "rtmp_url": "rtmp://localhost:1935/live/camera_1",
    "fps": 25
  },
  "alarm": {
    "enable": true,
    "hook_url": "http://localhost:48080/admin-api/device/alarm",
    "confidence_threshold": 0.6,
    "cooldown_time": 10
  },
  "regions": [
    {
      "region_id": "entrance",
      "polygon": [[100,100], [500,100], [500,400], [100,400]]
    }
  ]
}
```

**响应**:
```json
{
  "code": 0,
  "msg": "Config generated successfully",
  "config_path": "config/task1.ini"
}
```

### 9.2 DEVICE告警API

**Base URL**: `http://localhost:48080`  
**通过前端代理**: `/dev-api/device`

#### POST /admin-api/device/alarm - 设备告警回调

**请求**:
```json
{
  "device_id": "camera_001",
  "alarm_type": "ai_detection",
  "alarm_level": "warning",
  "alarm_content": "检测到未佩戴安全帽",
  "detections": [
    {
      "class_id": 0,
      "class_name": "no_helmet",
      "confidence": 0.85,
      "bbox": {
        "x": 100,
        "y": 200,
        "w": 50,
        "h": 100
      }
    }
  ],
  "region_id": "entrance",
  "timestamp": "2025-10-24 10:30:00"
}
```

**响应**:
```json
{
  "code": 0,
  "msg": "Alarm received successfully"
}
```

---

## 📈 第十部分：开发时间估算

### 总计工作量：11-16天（含ByteTrack追踪）

| 阶段 | 任务 | 工作量 | 优先级 |
|------|------|--------|--------|
| **阶段1** | **TASK模块补全** | **1天** | **P0** |
| | 区域过滤逻辑 | 0.5天 | P0 |
| | 资源释放完善 | 0.5天 | P1 |
| **阶段2** | **TaskManager开发** | **2-3天** | **P0** |
| | HTTP API实现 | 2天 | P0 |
| | 配置生成器 | 0.5天 | P0 |
| | 测试验证 | 0.5天 | P0 |
| ⭐ **阶段2.5** | **⭐ ByteTrack追踪集成** | **⭐ 3-4天** | **⭐ P0** |
| | 下载并集成ByteTrack C++代码 | 0.5天 | P0 |
| | 修改CMakeLists.txt、更新Config | 0.5天 | P0 |
| | 实现追踪逻辑（updateTracking） | 1天 | P0 |
| | 实现高级告警（停留/徘徊检测） | 1天 | P0 |
| | 测试验证（track_id稳定性） | 0.5-1天 | P0 |
| **阶段3** | **前端集成** | **2-3天** | **P0** |
| | API对接 | 1天 | P0 |
| | 扩展摄像头页面 | 1天 | P0 |
| | 网关路由配置 | 0.5天 | P0 |
| | 测试验证 | 0.5天 | P0 |
| **阶段4** | **视频墙** | **1-2天** | **P1** |
| | 视频墙页面开发 | 1天 | P1 |
| | WebSocket告警集成 | 0.5天 | P1 |
| | 性能优化 | 0.5天 | P1 |
| **阶段5** | **系统测试** | **2-3天** | **P0** |
| | 单路完整流程 | 0.5天 | P0 |
| | 10路并发测试 | 1天 | P0 |
| | 50路压力测试 | 1天 | P0 |
| | 文档完善 | 0.5天 | P1 |

---

## 🎉 第十一部分：总结

### 核心设计思想

1. **职责分离**：AI模块训练模型，TASK模块生产推理
2. **功能复用**：使用现有的告警接口、规则引擎、前端框架
3. **渐进开发**：先单路→再多路→最后视频墙
4. **性能优先**：C++实现，多进程并发，资源隔离

### 技术选型正确性

- ✅ **C++**: 高性能，适合实时处理
- ✅ **ONNX Runtime**: 跨平台，易集成
- ✅ **FFmpeg**: 成熟稳定，支持RTMP
- ✅ **cpp-httplib**: 轻量HTTP服务器
- ✅ **ZLMediaKit**: 高性能流媒体
- ✅ **Vue3 + Ant Design Vue**: 现代前端框架

### 完成后你将拥有

✅ 前端一键启动50路AI检测任务  
✅ 实时视频墙展示（1秒延迟）  
✅ 区域告警+规则引擎集成  
✅ 完整的商用级AI视频监控系统  
✅ 边缘离线部署能力  
✅ 可扩展的模块化架构

### 下一步行动

**立即开始（推荐顺序）**:
1. ✅ 完成区域过滤逻辑（0.5天）
2. ✅ 开发TaskManager HTTP API（2-3天）
3. ✅ 前端API对接（2-3天）
4. ✅ 视频墙页面（1-2天）
5. ✅ 50路压力测试（1天）

**准备好开始了吗？** 🚀

---

**文档结束** | 如有疑问请参考各子文档或提问

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 