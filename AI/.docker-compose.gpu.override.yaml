services:
  ai-service:
    runtime: nvidia
    environment:
      USE_GPU: "True"
      NVIDIA_VISIBLE_DEVICES: "all"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
