# Kafka bootstrap
KAFKA_BOOTSTRAP=127.0.0.1:9092

# transform-runtime 直连（默认）
TRANSFORM_API=http://127.0.0.1:48096

# 走网关时改为 1，并设置 GATEWAY_API
USE_GATEWAY=0
GATEWAY_API=http://127.0.0.1:48080/admin-api

# mock 外部系统接收端（与种子合同 endpoint 一致）
RECEIVER_HOST=127.0.0.1
RECEIVER_PORT=18080

HTTP_TIMEOUT=10
