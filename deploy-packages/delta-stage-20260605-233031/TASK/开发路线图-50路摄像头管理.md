# yFeiEye 50路摄像头AI检测系统 - 完整开发路线图

**目标**: 实现50路摄像头的前端配置、AI检测、实时播放和告警管理

**文档版本**: v1.0  
**创建日期**: 2025-10-24

---

## 🎯 系统架构对比

### 当前架构（VIDEO模块）

```
摄像头(RTSP) → VIDEO(Python) → FFmpeg转码 → RTMP推流 → ZLMediaKit → HTTP-FLV → 前端播放
                     ↓
                只是转发视频
                没有AI处理
```

**前端页面**：`WEB/src/views/camera/index.vue`
- ✅ 摄像头列表展示
- ✅ 启动/停止RTSP转发
- ✅ 视频播放（DialogPlayer）
- ✅ 流状态监控
- ⚠️ **没有AI检测功能**

---

### 目标架构（TASK模块）

```
50路摄像头(RTSP) → TaskManager(C++) → 50个TASK实例(C++)
                         ↓                    ↓
                    HTTP API管理          YOLO推理+画框
                    (端口7000)                 ↓
                         ↓              RTMP推流(带检测框)
                         ↓                    ↓
                    前端配置管理          ZLMediaKit(80)
                         ↓                    ↓
                    启动/停止/查询        HTTP-FLV播放
                         ↓                    ↓
                    告警规则配置          前端视频墙展示
```

**新增功能**：
- ✅ **AI实时检测**（YOLOv11）
- ✅ **检测框标注**（OpenCV）
- ✅ **区域告警**（多边形ROI）
- ✅ **前端管理**（TaskManager API）
- ✅ **50路并发**（多TASK实例）

---

## 📊 你的前端页面长这样（现状分析）

### 1. 摄像头管理页面 (`camera/index.vue`)

**表格列**：
| 列名 | 数据 | 说明 |
|------|------|------|
| 设备ID | `id` | 唯一标识 |
| 设备名称 | `name` | 用户自定义 |
| 在线状态 | `online` | 绿色/红色标签 |
| 拉流地址 | `source` | RTSP URL |
| 推流地址 | `rtmp_stream` | RTMP URL（VIDEO模块推送） |
| **播放地址** | `http_stream` | **HTTP-FLV（前端播放）** |
| 流状态 | `stream_status` | running/stopped/error |

**操作按钮**：
- 🎬 播放RTMP流
- ▶️ 启动RTSP转发 → 调用 `startStreamForwarding(device_id)`
- ⏸️ 停止RTSP转发 → 调用 `stopStreamForwarding(device_id)`
- ✏️ 编辑 / 🗑️ 删除

**后端API**：
```typescript
// WEB/src/api/device/camera.ts
startStreamForwarding(device_id)  // POST /video/camera/device/{id}/stream/start
stopStreamForwarding(device_id)   // POST /video/camera/device/{id}/stream/stop
getStreamStatus(device_id)        // GET  /video/camera/device/{id}/stream/status
```

---

### 2. 视频播放器 (`DialogPlayer.vue`)

**播放器**：Jessibuca（支持HTTP-FLV）

**显示内容**：
- 视频画面（实时流）
- 播放地址（可复制）
- PTZ云台控制（上下左右、变焦）

---

## 🚀 完整开发路线

### ✅ 阶段0：当前状态（已有基础）

你已经有：
- ✅ 完整的前端摄像头管理页面
- ✅ VIDEO模块（Python）的RTSP→RTMP转发
- ✅ ZLMediaKit流媒体服务器
- ✅ HTTP-FLV播放器（Jessibuca）
- ✅ TASK模块（C++）基础AI推理

---

### 🔨 阶段1：TASK模块补全核心功能（3-4天）

**目标**：让单个TASK实例能正常工作

#### Day 1-2: RTMP推流功能

**任务**：
```cpp
// 实现 TASK/src/RTMPEncoder.cpp
class RTMPEncoder {
    bool init(rtmpUrl, width, height, fps);
    bool encodeAndPush(cv::Mat frame);  // 推送带检测框的帧
    void release();
};
```

**集成到Detech.cpp**：
```cpp
// 在AI推理+画框之后
if (_config.enableRtmp && _rtmpEncoder) {
    _rtmpEncoder->encodeAndPush(frameWithBoxes);
}
```

**配置文件** (`config/task1.ini`)：
```ini
[rtmp]
enable=true
rtmp_url=rtmp://localhost:1935/live/camera_1
```

**验证**：
```bash
# 启动TASK
.\TASK.exe ..\..\config\task1.ini

# 播放验证（应该看到带检测框的视频）
ffplay rtmp://localhost:1935/live/camera_1

# 或在前端播放
http://localhost/live/camera_1.flv
```

---

#### Day 3: 区域检测（ROI）

**任务**：
```cpp
// 实现 TASK/src/AlarmRegion.cpp
struct AlarmRegion {
    std::string regionId;
    std::vector<cv::Point> polygon;
    std::vector<int> targetClasses;
    float confidenceThreshold;
};

bool isInAlarmRegion(const DetectObject& obj, const AlarmRegion& region) {
    cv::Point center(obj.box.x + obj.box.width/2, 
                     obj.box.y + obj.box.height/2);
    return cv::pointPolygonTest(region.polygon, center, false) >= 0;
}
```

**配置文件**：
```ini
[alarm_region_1]
enable=true
region_id=entrance
polygon=100,100;500,100;500,400;100,400
target_classes=0  # person类别
confidence_threshold=0.7
```

**验证**：
- 区域内检测 → 触发告警 ✅
- 区域外检测 → 不触发告警 ✅

---

#### Day 4: 资源释放完善

**任务**：
```cpp
void Detech::releaseResources() {
    // 释放FFmpeg、YOLO、RTMP编码器等资源
    if (_rtmpEncoder) {
        _rtmpEncoder->release();
        delete _rtmpEncoder;
    }
    // ...
}
```

**验证**：
```bash
# 长时间运行测试（24小时）
# 监控内存占用是否稳定
```

---

### 🔧 阶段2：TaskManager进程管理器（2-3天）

**目标**：实现HTTP API管理50个TASK实例

#### Day 5-6: TaskManager核心功能

**新建文件**：`TASK/src/TaskManager.cpp`

```cpp
class TaskManager {
private:
    std::map<int, std::unique_ptr<Detech>> tasks;  // task_id → TASK实例
    httplib::Server server;                        // HTTP服务器
    
public:
    TaskManager(int port = 7000);
    
    // API接口
    void startTask(int taskId, const std::string& configPath);
    void stopTask(int taskId);
    json getTaskStatus(int taskId);
    json getAllTasks();
    
    void run();  // 启动HTTP服务器
};
```

**HTTP API设计**：

| 接口 | 方法 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| `/task/start` | POST | 启动TASK | `{"task_id": 1, "config_path": "config/task1.ini"}` | `{"code": 0, "msg": "启动成功"}` |
| `/task/stop` | POST | 停止TASK | `{"task_id": 1}` | `{"code": 0, "msg": "停止成功"}` |
| `/task/status` | GET | 查询状态 | `?task_id=1` | `{"code": 0, "data": {"status": "running", "fps": 25}}` |
| `/task/list` | GET | 任务列表 | 无 | `{"code": 0, "data": [{...}, {...}]}` |
| `/config/generate` | POST | 生成配置 | JSON配置 | `{"code": 0, "config_path": "config/task1.ini"}` |

**实现示例**：
```cpp
void TaskManager::setupRoutes() {
    // POST /task/start
    server.Post("/task/start", [this](const httplib::Request& req, httplib::Response& res) {
        auto json = nlohmann::json::parse(req.body);
        int taskId = json["task_id"];
        std::string configPath = json["config_path"];
        
        try {
            startTask(taskId, configPath);
            res.set_content(R"({"code": 0, "msg": "Task started"})", "application/json");
        } catch (const std::exception& e) {
            res.set_content(R"({"code": -1, "msg": ")" + std::string(e.what()) + R"("})", "application/json");
        }
    });
    
    // GET /task/status
    server.Get("/task/status", [this](const httplib::Request& req, httplib::Response& res) {
        int taskId = std::stoi(req.get_param_value("task_id"));
        auto status = getTaskStatus(taskId);
        res.set_content(status.dump(), "application/json");
    });
    
    // 更多路由...
}

void TaskManager::startTask(int taskId, const std::string& configPath) {
    if (tasks.find(taskId) != tasks.end()) {
        throw std::runtime_error("Task already running");
    }
    
    // 创建新的TASK实例
    auto task = std::make_unique<Detech>(configPath);
    task->init();
    task->runAsync();  // 异步运行
    
    tasks[taskId] = std::move(task);
    LOG(INFO) << "[TaskManager] Task " << taskId << " started";
}
```

**编译配置** (`CMakeLists.txt`)：
```cmake
# 添加TaskManager可执行文件
add_executable(TaskManager
    src/TaskManager.cpp
    src/Detech.cpp
    src/Yolov11Engine.cpp
    # ...
)

target_link_libraries(TaskManager
    cpp-httplib
    nlohmann_json
    # 其他库...
)
```

**验证**：
```bash
# 启动TaskManager
.\TaskManager.exe

# 测试API
curl -X POST http://localhost:7000/task/start \
  -H "Content-Type: application/json" \
  -d '{"task_id": 1, "config_path": "config/task1.ini"}'

# 查询状态
curl http://localhost:7000/task/status?task_id=1

# 任务列表
curl http://localhost:7000/task/list
```

---

#### Day 7: 配置文件生成器

**任务**：实现JSON→INI配置生成

```cpp
// POST /config/generate
server.Post("/config/generate", [](const httplib::Request& req, httplib::Response& res) {
    auto json = nlohmann::json::parse(req.body);
    
    int taskId = json["task_id"];
    std::string configPath = "config/task" + std::to_string(taskId) + ".ini";
    
    // 生成INI文件
    std::ofstream file(configPath);
    file << "[task]\n";
    file << "task_id=" << taskId << "\n\n";
    
    file << "[video]\n";
    file << "source=" << json["video"]["source"].get<std::string>() << "\n";
    file << "width=" << json["video"]["width"] << "\n";
    file << "height=" << json["video"]["height"] << "\n\n";
    
    file << "[model]\n";
    file << "model_path=" << json["model"]["model_path"].get<std::string>() << "\n";
    file << "confidence_threshold=" << json["model"]["confidence_threshold"] << "\n\n";
    
    file << "[rtmp]\n";
    file << "enable=true\n";
    file << "rtmp_url=" << json["rtmp"]["rtmp_url"].get<std::string>() << "\n\n";
    
    file << "[alarm]\n";
    file << "enable=" << (json["alarm"]["enable"].get<bool>() ? "true" : "false") << "\n";
    file << "hook_url=" << json["alarm"]["hook_url"].get<std::string>() << "\n";
    file << "confidence_threshold=" << json["alarm"]["confidence_threshold"] << "\n";
    file << "cooldown_time=" << json["alarm"]["cooldown_time"] << "\n\n";
    
    // 区域配置
    if (json.contains("regions")) {
        int regionIdx = 1;
        for (auto& region : json["regions"]) {
            file << "[alarm_region_" << regionIdx++ << "]\n";
            file << "enable=true\n";
            file << "region_id=" << region["region_id"].get<std::string>() << "\n";
            
            // polygon: [[x1,y1], [x2,y2], ...]  →  x1,y1;x2,y2;...
            std::string polygonStr;
            for (auto& point : region["polygon"]) {
                if (!polygonStr.empty()) polygonStr += ";";
                polygonStr += std::to_string(point[0].get<int>()) + "," + 
                              std::to_string(point[1].get<int>());
            }
            file << "polygon=" << polygonStr << "\n";
            
            // target_classes
            std::string classesStr;
            for (auto& cls : region["target_classes"]) {
                if (!classesStr.empty()) classesStr += ",";
                classesStr += std::to_string(cls.get<int>());
            }
            file << "target_classes=" << classesStr << "\n";
            file << "confidence_threshold=" << region["confidence_threshold"] << "\n\n";
        }
    }
    
    file.close();
    
    nlohmann::json response = {
        {"code", 0},
        {"msg", "Config file generated"},
        {"config_path", configPath}
    };
    res.set_content(response.dump(), "application/json");
});
```

**测试请求**：
```json
POST http://localhost:7000/config/generate
Content-Type: application/json

{
  "task_id": 1,
  "video": {
    "source": "rtsp://admin:Admin123@192.168.1.100:554/stream1",
    "width": 1920,
    "height": 1080
  },
  "model": {
    "model_path": "models/SafeHat.onnx",
    "confidence_threshold": 0.5
  },
  "rtmp": {
    "rtmp_url": "rtmp://localhost:1935/live/camera_1"
  },
  "alarm": {
    "enable": true,
    "hook_url": "http://localhost:48080/admin-api/alarm/callback/1",
    "confidence_threshold": 0.6,
    "cooldown_time": 10
  },
  "regions": [
    {
      "region_id": "entrance",
      "polygon": [[100,100], [500,100], [500,400], [100,400]],
      "target_classes": [0],
      "confidence_threshold": 0.7
    }
  ]
}
```

---

### 🎨 阶段3：前端集成TaskManager（2-3天）

**目标**：前端能通过Web界面管理50路TASK

#### Day 8-9: 前端API对接

**新建API文件**：`WEB/src/api/task/index.ts`

```typescript
import {defHttp} from '@/utils/http/axios';

// TaskManager API基础路径（通过代理）
const TASK_API_BASE = '/dev-api/task';

// ====================== TASK管理接口 ======================

export interface TaskConfig {
  task_id: number;
  video: {
    source: string;
    width: number;
    height: number;
  };
  model: {
    model_path: string;
    confidence_threshold: number;
  };
  rtmp: {
    rtmp_url: string;
  };
  alarm: {
    enable: boolean;
    hook_url: string;
    confidence_threshold: number;
    cooldown_time: number;
  };
  regions?: Array<{
    region_id: string;
    polygon: number[][];
    target_classes: number[];
    confidence_threshold: number;
  }>;
}

export interface TaskStatus {
  task_id: number;
  status: 'running' | 'stopped' | 'error';
  fps: number;
  frame_count: number;
  uptime: number;
  rtmp_url: string;
}

// 启动TASK
export const startTask = (taskId: number, configPath: string) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/task/start`,
    data: { task_id: taskId, config_path: configPath }
  });
};

// 停止TASK
export const stopTask = (taskId: number) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/task/stop`,
    data: { task_id: taskId }
  });
};

// 查询TASK状态
export const getTaskStatus = (taskId: number) => {
  return defHttp.get<TaskStatus>({
    url: `${TASK_API_BASE}/task/status`,
    params: { task_id: taskId }
  });
};

// 获取所有TASK列表
export const getTaskList = () => {
  return defHttp.get<TaskStatus[]>({
    url: `${TASK_API_BASE}/task/list`
  });
};

// 生成配置文件
export const generateConfig = (config: TaskConfig) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/config/generate`,
    data: config
  });
};
```

**配置前端代理**：`WEB/.env.development`

```javascript
VITE_PROXY = [
  ["/api/v1/buckets", "http://localhost:9000/api/v1/buckets"],
  ["/dev-api/nodeRed", "http://127.0.0.1:1880"],
  ["/dev-api/task", "http://127.0.0.1:7000"],        // 新增TaskManager代理
  ["/dev-api", "http://127.0.0.1:48080/admin-api"]
]
```

---

#### Day 10: 前端页面扩展

**方案1：扩展现有摄像头页面**

在 `WEB/src/views/camera/index.vue` 中添加"AI任务"功能：

```vue
<template>
  <div class="camera-container">
    <!-- 原有摄像头列表 -->
    <BasicTable @register="registerTable">
      <!-- ... -->
      <template #bodyCell="{ column, record }">
        <!-- 新增操作按钮 -->
        <template v-if="column.dataIndex === 'action'">
          <TableAction :actions="getTableActions(record)" />
        </template>
      </template>
    </BasicTable>
  </div>
</template>

<script lang="ts" setup>
import { startTask, stopTask, getTaskStatus } from '@/api/task';

// 扩展操作按钮
const getTableActions = (record) => {
  const actions = [
    {
      icon: 'octicon:play-16',
      tooltip: '播放视频',
      onClick: () => handlePlay(record)
    },
    {
      icon: 'ant-design:robot-outlined',
      tooltip: '启动AI检测',
      onClick: () => handleStartAI(record)  // 新增：启动AI任务
    },
    {
      icon: 'ant-design:stop-outlined',
      tooltip: '停止AI检测',
      onClick: () => handleStopAI(record)   // 新增：停止AI任务
    },
    // ... 其他按钮
  ];
  return actions;
};

// 启动AI检测任务
const handleStartAI = async (record) => {
  try {
    createMessage.loading({ content: '正在启动AI检测...', key: 'ai' });
    
    // 1. 生成配置文件
    const config = {
      task_id: parseInt(record.id),
      video: {
        source: record.source,  // RTSP地址
        width: 1920,
        height: 1080
      },
      model: {
        model_path: 'models/SafeHat.onnx',
        confidence_threshold: 0.5
      },
      rtmp: {
        rtmp_url: `rtmp://localhost:1935/live/ai_camera_${record.id}`
      },
      alarm: {
        enable: true,
        hook_url: `http://localhost:48080/admin-api/alarm/callback/${record.id}`,
        confidence_threshold: 0.6,
        cooldown_time: 10
      }
    };
    
    const configRes = await generateConfig(config);
    
    // 2. 启动TASK
    await startTask(parseInt(record.id), configRes.data.config_path);
    
    createMessage.success({ content: 'AI检测已启动', key: 'ai' });
    
    // 3. 更新播放地址为AI推流地址
    record.http_stream = `http://localhost/live/ai_camera_${record.id}.flv`;
    reload();
    
  } catch (error) {
    console.error('启动AI检测失败', error);
    createMessage.error({ content: '启动AI检测失败', key: 'ai' });
  }
};

// 停止AI检测任务
const handleStopAI = async (record) => {
  try {
    createMessage.loading({ content: '正在停止AI检测...', key: 'ai' });
    await stopTask(parseInt(record.id));
    createMessage.success({ content: 'AI检测已停止', key: 'ai' });
    reload();
  } catch (error) {
    console.error('停止AI检测失败', error);
    createMessage.error({ content: '停止AI检测失败', key: 'ai' });
  }
};
</script>
```

**效果**：
- 每个摄像头都有"启动AI检测"和"停止AI检测"按钮
- 点击"启动AI检测" → 自动生成配置 → 启动TASK实例 → 播放地址切换为AI流
- 点击"播放" → 看到带检测框的实时画面

---

**方案2：新建AI任务管理页面**（推荐）

新建文件：`WEB/src/views/ai-task/index.vue`

```vue
<template>
  <div class="ai-task-container">
    <BasicTable @register="registerTable">
      <template #toolbar>
        <a-button type="primary" @click="openConfigModal">
          <template #icon>
            <RobotOutlined/>
          </template>
          新建AI检测任务
        </a-button>
        <a-button @click="reloadAllStatus">
          <template #icon>
            <SyncOutlined/>
          </template>
          刷新状态
        </a-button>
      </template>

      <template #bodyCell="{ column, record }">
        <!-- AI任务状态 -->
        <template v-if="column.dataIndex === 'status'">
          <a-tag :color="getStatusColor(record.status)">
            {{ getStatusText(record.status) }}
          </a-tag>
        </template>

        <!-- FPS显示 -->
        <template v-else-if="column.dataIndex === 'fps'">
          <span>{{ record.fps }} fps</span>
        </template>

        <!-- 操作 -->
        <template v-else-if="column.dataIndex === 'action'">
          <TableAction :actions="getTableActions(record)" />
        </template>
      </template>
    </BasicTable>

    <!-- AI任务配置弹窗 -->
    <AITaskConfigModal @register="registerConfigModal" @success="handleConfigSuccess"/>
  </div>
</template>

<script lang="ts" setup>
import {onMounted, ref} from 'vue';
import {BasicTable, TableAction, useTable} from '@/components/Table';
import {useMessage} from '@/hooks/web/useMessage';
import {useModal} from "@/components/Modal";
import {
  startTask,
  stopTask,
  getTaskList,
  getTaskStatus,
  generateConfig
} from '@/api/task';
import {RobotOutlined, SyncOutlined} from '@ant-design/icons-vue';
import AITaskConfigModal from './components/AITaskConfigModal.vue';

const {createMessage} = useMessage();
const [registerConfigModal, {openModal: openConfigModal}] = useModal();

// 表格列定义
const columns = [
  { title: '任务ID', dataIndex: 'task_id', width: 80 },
  { title: '摄像头名称', dataIndex: 'camera_name', width: 150 },
  { title: '状态', dataIndex: 'status', width: 100 },
  { title: 'FPS', dataIndex: 'fps', width: 80 },
  { title: '处理帧数', dataIndex: 'frame_count', width: 100 },
  { title: '运行时长', dataIndex: 'uptime', width: 100 },
  { title: 'RTMP推流地址', dataIndex: 'rtmp_url', width: 200 },
  { title: '操作', dataIndex: 'action', width: 200 },
];

const [registerTable, {reload}] = useTable({
  title: 'AI检测任务列表',
  api: getTaskList,
  columns: columns,
  pagination: true,
  rowKey: 'task_id',
});

// 获取操作按钮
const getTableActions = (record) => {
  const actions = [];
  
  if (record.status === 'running') {
    actions.push({
      icon: 'ant-design:pause-circle-outlined',
      tooltip: '停止任务',
      onClick: () => handleStop(record.task_id)
    });
  } else {
    actions.push({
      icon: 'ant-design:play-circle-outlined',
      tooltip: '启动任务',
      onClick: () => handleStart(record.task_id)
    });
  }
  
  actions.push(
    {
      icon: 'octicon:play-16',
      tooltip: '播放视频',
      onClick: () => handlePlay(record)
    },
    {
      icon: 'ant-design:setting-outlined',
      tooltip: '配置',
      onClick: () => openConfigModal(record)
    },
    {
      icon: 'material-symbols:delete-outline-rounded',
      tooltip: '删除',
      popConfirm: {
        title: '确定删除此任务？',
        confirm: () => handleDelete(record.task_id)
      }
    }
  );
  
  return actions;
};

// 启动任务
const handleStart = async (taskId: number) => {
  try {
    await startTask(taskId, `config/task${taskId}.ini`);
    createMessage.success('任务启动成功');
    reload();
  } catch (error) {
    createMessage.error('任务启动失败');
  }
};

// 停止任务
const handleStop = async (taskId: number) => {
  try {
    await stopTask(taskId);
    createMessage.success('任务停止成功');
    reload();
  } catch (error) {
    createMessage.error('任务停止失败');
  }
};

// 状态文本和颜色
const getStatusText = (status: string) => {
  const map = {
    'running': '运行中',
    'stopped': '已停止',
    'error': '错误'
  };
  return map[status] || status;
};

const getStatusColor = (status: string) => {
  const map = {
    'running': 'green',
    'stopped': 'red',
    'error': 'orange'
  };
  return map[status] || 'default';
};
</script>
```

**新建配置弹窗**：`WEB/src/views/ai-task/components/AITaskConfigModal.vue`

```vue
<template>
  <BasicModal
    @register="register"
    title="配置AI检测任务"
    width="800px"
    @ok="handleSubmit"
  >
    <BasicForm @register="registerForm" />
  </BasicModal>
</template>

<script lang="ts" setup>
import {BasicModal, useModalInner} from '@/components/Modal';
import {BasicForm, useForm} from '@/components/Form';
import {generateConfig, startTask} from '@/api/task';
import {useMessage} from '@/hooks/web/useMessage';

const {createMessage} = useMessage();
const emit = defineEmits(['success', 'register']);

const [registerForm, {validate, setFieldsValue, resetFields}] = useForm({
  labelWidth: 120,
  schemas: [
    {
      field: 'task_id',
      label: '任务ID',
      component: 'InputNumber',
      required: true,
    },
    {
      field: 'camera_id',
      label: '选择摄像头',
      component: 'Select',
      componentProps: {
        options: [],  // 从摄像头列表获取
        showSearch: true,
      },
      required: true,
    },
    {
      field: 'model_path',
      label: '检测模型',
      component: 'Select',
      componentProps: {
        options: [
          { label: '安全帽检测', value: 'models/SafeHat.onnx' },
          { label: '跌倒检测', value: 'models/FallDetect.onnx' },
          { label: '通用目标检测', value: 'models/yolov11n.onnx' },
        ],
      },
      required: true,
      defaultValue: 'models/SafeHat.onnx',
    },
    {
      field: 'confidence_threshold',
      label: '检测阈值',
      component: 'Slider',
      componentProps: {
        min: 0,
        max: 1,
        step: 0.05,
        marks: {
          0.5: '0.5',
          0.7: '0.7',
        },
      },
      defaultValue: 0.6,
    },
    {
      field: 'enable_alarm',
      label: '启用告警',
      component: 'Switch',
      defaultValue: true,
    },
    {
      field: 'alarm_confidence',
      label: '告警阈值',
      component: 'Slider',
      componentProps: {
        min: 0,
        max: 1,
        step: 0.05,
      },
      defaultValue: 0.7,
      ifShow: ({values}) => values.enable_alarm,
    },
    {
      field: 'cooldown_time',
      label: '告警冷却时间(秒)',
      component: 'InputNumber',
      componentProps: {
        min: 1,
        max: 60,
      },
      defaultValue: 10,
      ifShow: ({values}) => values.enable_alarm,
    },
  ],
  showActionButtonGroup: false,
});

const [register, {closeModal}] = useModalInner((data) => {
  resetFields();
  if (data) {
    setFieldsValue(data);
  }
});

const handleSubmit = async () => {
  try {
    const values = await validate();
    
    // 构建配置
    const config = {
      task_id: values.task_id,
      video: {
        source: values.camera_source,  // 从摄像头信息获取
        width: 1920,
        height: 1080,
      },
      model: {
        model_path: values.model_path,
        confidence_threshold: values.confidence_threshold,
      },
      rtmp: {
        rtmp_url: `rtmp://localhost:1935/live/ai_task_${values.task_id}`,
      },
      alarm: {
        enable: values.enable_alarm,
        hook_url: `http://localhost:48080/admin-api/alarm/callback/${values.task_id}`,
        confidence_threshold: values.alarm_confidence,
        cooldown_time: values.cooldown_time,
      },
    };
    
    // 1. 生成配置文件
    const configRes = await generateConfig(config);
    
    // 2. 启动任务
    await startTask(values.task_id, configRes.data.config_path);
    
    createMessage.success('AI任务启动成功');
    emit('success');
    closeModal();
    
  } catch (error) {
    createMessage.error('配置失败');
  }
};
</script>
```

---

### 🎬 阶段4：视频墙展示（1-2天）

**目标**：同时展示多路AI检测视频

新建页面：`WEB/src/views/ai-task/video-wall.vue`

```vue
<template>
  <div class="video-wall-container">
    <div class="video-wall-header">
      <h2>AI检测视频墙（{{ runningTasks.length }}/50路）</h2>
      <a-radio-group v-model:value="gridLayout" button-style="solid">
        <a-radio-button value="2x2">2x2</a-radio-button>
        <a-radio-button value="3x3">3x3</a-radio-button>
        <a-radio-button value="4x4">4x4</a-radio-button>
        <a-radio-button value="5x5">5x5</a-radio-button>
      </a-radio-group>
    </div>
    
    <div :class="`video-wall-grid grid-${gridLayout}`">
      <div 
        v-for="task in runningTasks" 
        :key="task.task_id"
        class="video-wall-item"
      >
        <!-- 视频播放器 -->
        <Jessibuca 
          :playUrl="task.http_flv_url" 
          :hasAudio="false"
          class="video-player"
        />
        
        <!-- 信息覆盖层 -->
        <div class="video-info-overlay">
          <div class="video-title">{{ task.camera_name }}</div>
          <div class="video-stats">
            <span class="fps">{{ task.fps }} FPS</span>
            <span class="status" :class="task.status">{{ task.status }}</span>
          </div>
        </div>
        
        <!-- 告警标识 -->
        <div v-if="task.has_alarm" class="alarm-indicator">
          <WarningOutlined /> 告警中
        </div>
      </div>
    </div>
  </div>
</template>

<script lang="ts" setup>
import {ref, onMounted, onUnmounted} from 'vue';
import {getTaskList} from '@/api/task';
import Jessibuca from '@/components/Player/module/jessibuca.vue';
import {WarningOutlined} from '@ant-design/icons-vue';

const gridLayout = ref('3x3');
const runningTasks = ref([]);
const updateTimer = ref(null);

// 获取运行中的任务
const fetchRunningTasks = async () => {
  try {
    const response = await getTaskList();
    runningTasks.value = response.data
      .filter(task => task.status === 'running')
      .map(task => ({
        ...task,
        http_flv_url: `http://localhost/live/ai_task_${task.task_id}.flv`,
      }));
  } catch (error) {
    console.error('获取任务列表失败', error);
  }
};

onMounted(() => {
  fetchRunningTasks();
  // 每5秒更新一次状态
  updateTimer.value = setInterval(fetchRunningTasks, 5000);
});

onUnmounted(() => {
  if (updateTimer.value) {
    clearInterval(updateTimer.value);
  }
});
</script>

<style scoped lang="less">
.video-wall-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #000;
}

.video-wall-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  background: #1a1a1a;
  color: #fff;
}

.video-wall-grid {
  flex: 1;
  display: grid;
  gap: 8px;
  padding: 8px;
  
  &.grid-2x2 { grid-template-columns: repeat(2, 1fr); }
  &.grid-3x3 { grid-template-columns: repeat(3, 1fr); }
  &.grid-4x4 { grid-template-columns: repeat(4, 1fr); }
  &.grid-5x5 { grid-template-columns: repeat(5, 1fr); }
}

.video-wall-item {
  position: relative;
  background: #222;
  border-radius: 4px;
  overflow: hidden;
  
  &:hover .video-info-overlay {
    opacity: 1;
  }
}

.video-player {
  width: 100%;
  height: 100%;
}

.video-info-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
  padding: 8px;
  opacity: 0.7;
  transition: opacity 0.3s;
  
  .video-title {
    color: #fff;
    font-size: 14px;
    font-weight: bold;
  }
  
  .video-stats {
    display: flex;
    justify-content: space-between;
    margin-top: 4px;
    
    .fps {
      color: #4caf50;
      font-size: 12px;
    }
    
    .status {
      font-size: 12px;
      &.running { color: #4caf50; }
      &.error { color: #f44336; }
    }
  }
}

.alarm-indicator {
  position: absolute;
  top: 8px;
  right: 8px;
  background: #f44336;
  color: #fff;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0.3; }
}
</style>
```

---

## 📋 最终效果

### 1. 摄像头管理页面 (`/camera`)
- ✅ 管理所有摄像头设备（ONVIF扫描、新增、编辑、删除）
- ✅ 启动/停止RTSP转发（VIDEO模块）
- ✅ **新增：启动/停止AI检测（TASK模块）**

### 2. AI任务管理页面 (`/ai-task`)
- ✅ AI任务列表（50路）
- ✅ 新建AI任务（配置检测模型、阈值、告警规则、ROI区域）
- ✅ 启动/停止/删除任务
- ✅ 实时状态监控（FPS、帧数、运行时长）

### 3. 视频墙页面 (`/ai-task/video-wall`)
- ✅ 同时展示多路AI检测视频（2x2 / 3x3 / 4x4 / 5x5布局）
- ✅ 每路视频显示：摄像头名称、FPS、运行状态
- ✅ 告警高亮显示

### 4. 完整数据流

```
用户操作前端 → TaskManager(7000) → TASK实例(C++) → YOLO推理+画框
                    ↓                       ↓
              生成配置文件            RTMP推流(带检测框)
                                           ↓
                                    ZLMediaKit(80)
                                           ↓
                                    HTTP-FLV播放
                                           ↓
                                    前端视频墙展示
```

---

## 🎯 50路摄像头部署方案

### 硬件要求

| 组件 | 配置 | 说明 |
|------|------|------|
| **CPU** | Intel i7-12700 或更高 | 50路AI推理需要强CPU |
| **内存** | 32GB+ | 每路TASK约500MB，50路需25GB+ |
| **存储** | 500GB SSD | 模型、配置、日志 |
| **网络** | 千兆网络 | 50路视频流带宽需求大 |
| **GPU（可选）** | RTX 4070 或更高 | 加速AI推理（可选，CPU也能跑） |

### 软件架构

```
边缘服务器（一台物理机）
├── ZLMediaKit（流媒体服务器）- 1个实例
├── TaskManager（进程管理器）- 1个实例
│   ├── TASK实例 #1（camera_1）
│   ├── TASK实例 #2（camera_2）
│   ├── ...
│   └── TASK实例 #50（camera_50）
└── PostgreSQL + MinIO + Node-RED（后端服务）
```

**注意**：
- 50个TASK实例都在同一台机器上运行
- TaskManager统一管理50个进程的生命周期
- 每个TASK独立配置文件，独立RTMP流

### 部署步骤

#### Step 1: 启动ZLMediaKit

```bash
cd F:\EASYLOT\zlmediakit
.\MediaServer.exe -c config.ini
```

#### Step 2: 启动TaskManager

```bash
cd F:\EASYLOT\yfeieye-main\TASK\build\Release
.\TaskManager.exe
```

TaskManager会：
- 监听7000端口
- 等待前端发送任务启动请求

#### Step 3: 在前端配置50路摄像头

```typescript
// 批量创建任务示例
const cameras = [
  { id: 1, name: '大门', rtsp: 'rtsp://192.168.1.101:554/stream1' },
  { id: 2, name: '车库', rtsp: 'rtsp://192.168.1.102:554/stream1' },
  // ... 48 more cameras
];

for (let camera of cameras) {
  await generateConfig({
    task_id: camera.id,
    video: { source: camera.rtsp, width: 1920, height: 1080 },
    model: { model_path: 'models/SafeHat.onnx', confidence_threshold: 0.5 },
    rtmp: { rtmp_url: `rtmp://localhost:1935/live/camera_${camera.id}` },
    alarm: {
      enable: true,
      hook_url: `http://localhost:48080/admin-api/alarm/callback/${camera.id}`,
      confidence_threshold: 0.6,
      cooldown_time: 10
    }
  });
  
  await startTask(camera.id, `config/task${camera.id}.ini`);
}
```

#### Step 4: 打开视频墙

访问：`http://localhost:8099/ai-task/video-wall`

选择5x5布局 → 同时看到25路视频（可滚动查看全部50路）

---

## 📊 性能评估

### 单路TASK资源占用

| 指标 | CPU模式 | GPU模式 |
|------|---------|---------|
| **CPU占用** | ~10% (12核心) | ~5% |
| **内存占用** | ~500MB | ~500MB |
| **显存占用** | N/A | ~2GB |
| **推理速度** | 25-30 FPS | 100+ FPS |
| **延迟** | ~1秒 | ~0.5秒 |

### 50路并发预估

| 资源 | CPU模式 | GPU模式 |
|------|---------|---------|
| **CPU占用** | ~80% (12核心) | ~40% |
| **内存占用** | ~25GB | ~25GB |
| **显存占用** | N/A | ~8GB (RTX 4070) |
| **网络带宽** | ~200Mbps (出口) | ~200Mbps |

**结论**：
- CPU模式可以跑50路（但CPU会满载）
- GPU模式更轻松（推荐）

---

## ✅ 验收标准

### 功能验收

- [ ] 单个TASK实例正常运行（RTSP→AI→RTMP→播放）
- [ ] TaskManager成功管理50个TASK实例
- [ ] 前端能通过API启动/停止任意TASK
- [ ] 前端能实时查看所有TASK状态
- [ ] 视频墙能同时播放多路视频
- [ ] 区域告警正常触发（区域内有人→告警，区域外无告警）
- [ ] 告警回调到DEVICE模块，触发Node-RED规则

### 性能验收

- [ ] 单路TASK FPS ≥ 20
- [ ] 50路TASK同时运行，系统稳定
- [ ] 视频延迟 ≤ 2秒
- [ ] 内存无泄漏（24小时运行内存增长 < 10%）
- [ ] CPU占用合理（CPU模式 < 90%）

### 用户体验验收

- [ ] 前端操作流畅（启动任务 < 3秒）
- [ ] 视频墙布局可调整（2x2 / 3x3 / 4x4 / 5x5）
- [ ] 告警实时显示（< 1秒延迟）
- [ ] 日志可查询（Glog输出）

---

## 🔄 后续优化方向

### 短期优化（1-2周）

1. **性能监控面板**
   - 实时CPU/内存/FPS图表
   - 告警统计（每小时/每天）
   - 系统健康度评分

2. **多模型支持**
   - 运行时切换模型（安全帽→跌倒检测）
   - 模型热加载

3. **类别文件加载**
   - 支持中文标签
   - 自定义检测类别

### 中期优化（1个月）

1. **分布式部署**
   - 多台边缘服务器
   - 负载均衡

2. **录像回放**
   - 保存检测视频
   - 事件回放

3. **高级告警规则**
   - 人数统计告警
   - 区域入侵时长告警
   - 跨区域追踪告警

### 长期优化（3个月）

1. **GPU加速完善**
   - 支持CUDA 12.8
   - 或DirectML

2. **云端集成**
   - 边缘-云协同
   - 模型远程更新

3. **智能分析**
   - 行为分析（徘徊、奔跑）
   - 轨迹分析
   - 热力图

---

## 📚 参考文档

- [TASK模块开发指引.md](./TASK模块开发指引.md) - 详细技术实现
- [架构分析与开发路线图.md](./架构分析与开发路线图.md) - 系统架构分析
- [生产环境使用指南.md](./生产环境使用指南.md) - HTTP-FLV部署指南

---

## 🎉 总结

**你的C++ TASK模块 = VIDEO模块 + AI能力**

| 对比项 | VIDEO (Python) | TASK (C++) |
|--------|---------------|-----------|
| 功能 | 仅转发视频 | ✅ AI检测+画框+告警 |
| 性能 | 中等 | ✅ 高性能（C++原生） |
| 管理 | 手动 | ✅ 前端API控制 |
| 并发 | ~10路 | ✅ 50路+ |
| 告警 | ❌ | ✅ 区域检测+规则引擎 |

**开发时间估算**：
- 阶段1（TASK补全）：3-4天
- 阶段2（TaskManager）：2-3天
- 阶段3（前端集成）：2-3天
- 阶段4（视频墙）：1-2天
- **总计：8-12天**

**完成后，你将拥有**：
✅ 前端一键启动50路AI检测任务  
✅ 实时视频墙展示  
✅ 区域告警+规则引擎集成  
✅ 完整的商用级AI视频监控系统  

---

**准备好开始了吗？** 🚀


**目标**: 实现50路摄像头的前端配置、AI检测、实时播放和告警管理

**文档版本**: v1.0  
**创建日期**: 2025-10-24

---

## 🎯 系统架构对比

### 当前架构（VIDEO模块）

```
摄像头(RTSP) → VIDEO(Python) → FFmpeg转码 → RTMP推流 → ZLMediaKit → HTTP-FLV → 前端播放
                     ↓
                只是转发视频
                没有AI处理
```

**前端页面**：`WEB/src/views/camera/index.vue`
- ✅ 摄像头列表展示
- ✅ 启动/停止RTSP转发
- ✅ 视频播放（DialogPlayer）
- ✅ 流状态监控
- ⚠️ **没有AI检测功能**

---

### 目标架构（TASK模块）

```
50路摄像头(RTSP) → TaskManager(C++) → 50个TASK实例(C++)
                         ↓                    ↓
                    HTTP API管理          YOLO推理+画框
                    (端口7000)                 ↓
                         ↓              RTMP推流(带检测框)
                         ↓                    ↓
                    前端配置管理          ZLMediaKit(80)
                         ↓                    ↓
                    启动/停止/查询        HTTP-FLV播放
                         ↓                    ↓
                    告警规则配置          前端视频墙展示
```

**新增功能**：
- ✅ **AI实时检测**（YOLOv11）
- ✅ **检测框标注**（OpenCV）
- ✅ **区域告警**（多边形ROI）
- ✅ **前端管理**（TaskManager API）
- ✅ **50路并发**（多TASK实例）

---

## 📊 你的前端页面长这样（现状分析）

### 1. 摄像头管理页面 (`camera/index.vue`)

**表格列**：
| 列名 | 数据 | 说明 |
|------|------|------|
| 设备ID | `id` | 唯一标识 |
| 设备名称 | `name` | 用户自定义 |
| 在线状态 | `online` | 绿色/红色标签 |
| 拉流地址 | `source` | RTSP URL |
| 推流地址 | `rtmp_stream` | RTMP URL（VIDEO模块推送） |
| **播放地址** | `http_stream` | **HTTP-FLV（前端播放）** |
| 流状态 | `stream_status` | running/stopped/error |

**操作按钮**：
- 🎬 播放RTMP流
- ▶️ 启动RTSP转发 → 调用 `startStreamForwarding(device_id)`
- ⏸️ 停止RTSP转发 → 调用 `stopStreamForwarding(device_id)`
- ✏️ 编辑 / 🗑️ 删除

**后端API**：
```typescript
// WEB/src/api/device/camera.ts
startStreamForwarding(device_id)  // POST /video/camera/device/{id}/stream/start
stopStreamForwarding(device_id)   // POST /video/camera/device/{id}/stream/stop
getStreamStatus(device_id)        // GET  /video/camera/device/{id}/stream/status
```

---

### 2. 视频播放器 (`DialogPlayer.vue`)

**播放器**：Jessibuca（支持HTTP-FLV）

**显示内容**：
- 视频画面（实时流）
- 播放地址（可复制）
- PTZ云台控制（上下左右、变焦）

---

## 🚀 完整开发路线

### ✅ 阶段0：当前状态（已有基础）

你已经有：
- ✅ 完整的前端摄像头管理页面
- ✅ VIDEO模块（Python）的RTSP→RTMP转发
- ✅ ZLMediaKit流媒体服务器
- ✅ HTTP-FLV播放器（Jessibuca）
- ✅ TASK模块（C++）基础AI推理

---

### 🔨 阶段1：TASK模块补全核心功能（3-4天）

**目标**：让单个TASK实例能正常工作

#### Day 1-2: RTMP推流功能

**任务**：
```cpp
// 实现 TASK/src/RTMPEncoder.cpp
class RTMPEncoder {
    bool init(rtmpUrl, width, height, fps);
    bool encodeAndPush(cv::Mat frame);  // 推送带检测框的帧
    void release();
};
```

**集成到Detech.cpp**：
```cpp
// 在AI推理+画框之后
if (_config.enableRtmp && _rtmpEncoder) {
    _rtmpEncoder->encodeAndPush(frameWithBoxes);
}
```

**配置文件** (`config/task1.ini`)：
```ini
[rtmp]
enable=true
rtmp_url=rtmp://localhost:1935/live/camera_1
```

**验证**：
```bash
# 启动TASK
.\TASK.exe ..\..\config\task1.ini

# 播放验证（应该看到带检测框的视频）
ffplay rtmp://localhost:1935/live/camera_1

# 或在前端播放
http://localhost/live/camera_1.flv
```

---

#### Day 3: 区域检测（ROI）

**任务**：
```cpp
// 实现 TASK/src/AlarmRegion.cpp
struct AlarmRegion {
    std::string regionId;
    std::vector<cv::Point> polygon;
    std::vector<int> targetClasses;
    float confidenceThreshold;
};

bool isInAlarmRegion(const DetectObject& obj, const AlarmRegion& region) {
    cv::Point center(obj.box.x + obj.box.width/2, 
                     obj.box.y + obj.box.height/2);
    return cv::pointPolygonTest(region.polygon, center, false) >= 0;
}
```

**配置文件**：
```ini
[alarm_region_1]
enable=true
region_id=entrance
polygon=100,100;500,100;500,400;100,400
target_classes=0  # person类别
confidence_threshold=0.7
```

**验证**：
- 区域内检测 → 触发告警 ✅
- 区域外检测 → 不触发告警 ✅

---

#### Day 4: 资源释放完善

**任务**：
```cpp
void Detech::releaseResources() {
    // 释放FFmpeg、YOLO、RTMP编码器等资源
    if (_rtmpEncoder) {
        _rtmpEncoder->release();
        delete _rtmpEncoder;
    }
    // ...
}
```

**验证**：
```bash
# 长时间运行测试（24小时）
# 监控内存占用是否稳定
```

---

### 🔧 阶段2：TaskManager进程管理器（2-3天）

**目标**：实现HTTP API管理50个TASK实例

#### Day 5-6: TaskManager核心功能

**新建文件**：`TASK/src/TaskManager.cpp`

```cpp
class TaskManager {
private:
    std::map<int, std::unique_ptr<Detech>> tasks;  // task_id → TASK实例
    httplib::Server server;                        // HTTP服务器
    
public:
    TaskManager(int port = 7000);
    
    // API接口
    void startTask(int taskId, const std::string& configPath);
    void stopTask(int taskId);
    json getTaskStatus(int taskId);
    json getAllTasks();
    
    void run();  // 启动HTTP服务器
};
```

**HTTP API设计**：

| 接口 | 方法 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| `/task/start` | POST | 启动TASK | `{"task_id": 1, "config_path": "config/task1.ini"}` | `{"code": 0, "msg": "启动成功"}` |
| `/task/stop` | POST | 停止TASK | `{"task_id": 1}` | `{"code": 0, "msg": "停止成功"}` |
| `/task/status` | GET | 查询状态 | `?task_id=1` | `{"code": 0, "data": {"status": "running", "fps": 25}}` |
| `/task/list` | GET | 任务列表 | 无 | `{"code": 0, "data": [{...}, {...}]}` |
| `/config/generate` | POST | 生成配置 | JSON配置 | `{"code": 0, "config_path": "config/task1.ini"}` |

**实现示例**：
```cpp
void TaskManager::setupRoutes() {
    // POST /task/start
    server.Post("/task/start", [this](const httplib::Request& req, httplib::Response& res) {
        auto json = nlohmann::json::parse(req.body);
        int taskId = json["task_id"];
        std::string configPath = json["config_path"];
        
        try {
            startTask(taskId, configPath);
            res.set_content(R"({"code": 0, "msg": "Task started"})", "application/json");
        } catch (const std::exception& e) {
            res.set_content(R"({"code": -1, "msg": ")" + std::string(e.what()) + R"("})", "application/json");
        }
    });
    
    // GET /task/status
    server.Get("/task/status", [this](const httplib::Request& req, httplib::Response& res) {
        int taskId = std::stoi(req.get_param_value("task_id"));
        auto status = getTaskStatus(taskId);
        res.set_content(status.dump(), "application/json");
    });
    
    // 更多路由...
}

void TaskManager::startTask(int taskId, const std::string& configPath) {
    if (tasks.find(taskId) != tasks.end()) {
        throw std::runtime_error("Task already running");
    }
    
    // 创建新的TASK实例
    auto task = std::make_unique<Detech>(configPath);
    task->init();
    task->runAsync();  // 异步运行
    
    tasks[taskId] = std::move(task);
    LOG(INFO) << "[TaskManager] Task " << taskId << " started";
}
```

**编译配置** (`CMakeLists.txt`)：
```cmake
# 添加TaskManager可执行文件
add_executable(TaskManager
    src/TaskManager.cpp
    src/Detech.cpp
    src/Yolov11Engine.cpp
    # ...
)

target_link_libraries(TaskManager
    cpp-httplib
    nlohmann_json
    # 其他库...
)
```

**验证**：
```bash
# 启动TaskManager
.\TaskManager.exe

# 测试API
curl -X POST http://localhost:7000/task/start \
  -H "Content-Type: application/json" \
  -d '{"task_id": 1, "config_path": "config/task1.ini"}'

# 查询状态
curl http://localhost:7000/task/status?task_id=1

# 任务列表
curl http://localhost:7000/task/list
```

---

#### Day 7: 配置文件生成器

**任务**：实现JSON→INI配置生成

```cpp
// POST /config/generate
server.Post("/config/generate", [](const httplib::Request& req, httplib::Response& res) {
    auto json = nlohmann::json::parse(req.body);
    
    int taskId = json["task_id"];
    std::string configPath = "config/task" + std::to_string(taskId) + ".ini";
    
    // 生成INI文件
    std::ofstream file(configPath);
    file << "[task]\n";
    file << "task_id=" << taskId << "\n\n";
    
    file << "[video]\n";
    file << "source=" << json["video"]["source"].get<std::string>() << "\n";
    file << "width=" << json["video"]["width"] << "\n";
    file << "height=" << json["video"]["height"] << "\n\n";
    
    file << "[model]\n";
    file << "model_path=" << json["model"]["model_path"].get<std::string>() << "\n";
    file << "confidence_threshold=" << json["model"]["confidence_threshold"] << "\n\n";
    
    file << "[rtmp]\n";
    file << "enable=true\n";
    file << "rtmp_url=" << json["rtmp"]["rtmp_url"].get<std::string>() << "\n\n";
    
    file << "[alarm]\n";
    file << "enable=" << (json["alarm"]["enable"].get<bool>() ? "true" : "false") << "\n";
    file << "hook_url=" << json["alarm"]["hook_url"].get<std::string>() << "\n";
    file << "confidence_threshold=" << json["alarm"]["confidence_threshold"] << "\n";
    file << "cooldown_time=" << json["alarm"]["cooldown_time"] << "\n\n";
    
    // 区域配置
    if (json.contains("regions")) {
        int regionIdx = 1;
        for (auto& region : json["regions"]) {
            file << "[alarm_region_" << regionIdx++ << "]\n";
            file << "enable=true\n";
            file << "region_id=" << region["region_id"].get<std::string>() << "\n";
            
            // polygon: [[x1,y1], [x2,y2], ...]  →  x1,y1;x2,y2;...
            std::string polygonStr;
            for (auto& point : region["polygon"]) {
                if (!polygonStr.empty()) polygonStr += ";";
                polygonStr += std::to_string(point[0].get<int>()) + "," + 
                              std::to_string(point[1].get<int>());
            }
            file << "polygon=" << polygonStr << "\n";
            
            // target_classes
            std::string classesStr;
            for (auto& cls : region["target_classes"]) {
                if (!classesStr.empty()) classesStr += ",";
                classesStr += std::to_string(cls.get<int>());
            }
            file << "target_classes=" << classesStr << "\n";
            file << "confidence_threshold=" << region["confidence_threshold"] << "\n\n";
        }
    }
    
    file.close();
    
    nlohmann::json response = {
        {"code", 0},
        {"msg", "Config file generated"},
        {"config_path", configPath}
    };
    res.set_content(response.dump(), "application/json");
});
```

**测试请求**：
```json
POST http://localhost:7000/config/generate
Content-Type: application/json

{
  "task_id": 1,
  "video": {
    "source": "rtsp://admin:Admin123@192.168.1.100:554/stream1",
    "width": 1920,
    "height": 1080
  },
  "model": {
    "model_path": "models/SafeHat.onnx",
    "confidence_threshold": 0.5
  },
  "rtmp": {
    "rtmp_url": "rtmp://localhost:1935/live/camera_1"
  },
  "alarm": {
    "enable": true,
    "hook_url": "http://localhost:48080/admin-api/alarm/callback/1",
    "confidence_threshold": 0.6,
    "cooldown_time": 10
  },
  "regions": [
    {
      "region_id": "entrance",
      "polygon": [[100,100], [500,100], [500,400], [100,400]],
      "target_classes": [0],
      "confidence_threshold": 0.7
    }
  ]
}
```

---

### 🎨 阶段3：前端集成TaskManager（2-3天）

**目标**：前端能通过Web界面管理50路TASK

#### Day 8-9: 前端API对接

**新建API文件**：`WEB/src/api/task/index.ts`

```typescript
import {defHttp} from '@/utils/http/axios';

// TaskManager API基础路径（通过代理）
const TASK_API_BASE = '/dev-api/task';

// ====================== TASK管理接口 ======================

export interface TaskConfig {
  task_id: number;
  video: {
    source: string;
    width: number;
    height: number;
  };
  model: {
    model_path: string;
    confidence_threshold: number;
  };
  rtmp: {
    rtmp_url: string;
  };
  alarm: {
    enable: boolean;
    hook_url: string;
    confidence_threshold: number;
    cooldown_time: number;
  };
  regions?: Array<{
    region_id: string;
    polygon: number[][];
    target_classes: number[];
    confidence_threshold: number;
  }>;
}

export interface TaskStatus {
  task_id: number;
  status: 'running' | 'stopped' | 'error';
  fps: number;
  frame_count: number;
  uptime: number;
  rtmp_url: string;
}

// 启动TASK
export const startTask = (taskId: number, configPath: string) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/task/start`,
    data: { task_id: taskId, config_path: configPath }
  });
};

// 停止TASK
export const stopTask = (taskId: number) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/task/stop`,
    data: { task_id: taskId }
  });
};

// 查询TASK状态
export const getTaskStatus = (taskId: number) => {
  return defHttp.get<TaskStatus>({
    url: `${TASK_API_BASE}/task/status`,
    params: { task_id: taskId }
  });
};

// 获取所有TASK列表
export const getTaskList = () => {
  return defHttp.get<TaskStatus[]>({
    url: `${TASK_API_BASE}/task/list`
  });
};

// 生成配置文件
export const generateConfig = (config: TaskConfig) => {
  return defHttp.post({
    url: `${TASK_API_BASE}/config/generate`,
    data: config
  });
};
```

**配置前端代理**：`WEB/.env.development`

```javascript
VITE_PROXY = [
  ["/api/v1/buckets", "http://localhost:9000/api/v1/buckets"],
  ["/dev-api/nodeRed", "http://127.0.0.1:1880"],
  ["/dev-api/task", "http://127.0.0.1:7000"],        // 新增TaskManager代理
  ["/dev-api", "http://127.0.0.1:48080/admin-api"]
]
```

---

#### Day 10: 前端页面扩展

**方案1：扩展现有摄像头页面**

在 `WEB/src/views/camera/index.vue` 中添加"AI任务"功能：

```vue
<template>
  <div class="camera-container">
    <!-- 原有摄像头列表 -->
    <BasicTable @register="registerTable">
      <!-- ... -->
      <template #bodyCell="{ column, record }">
        <!-- 新增操作按钮 -->
        <template v-if="column.dataIndex === 'action'">
          <TableAction :actions="getTableActions(record)" />
        </template>
      </template>
    </BasicTable>
  </div>
</template>

<script lang="ts" setup>
import { startTask, stopTask, getTaskStatus } from '@/api/task';

// 扩展操作按钮
const getTableActions = (record) => {
  const actions = [
    {
      icon: 'octicon:play-16',
      tooltip: '播放视频',
      onClick: () => handlePlay(record)
    },
    {
      icon: 'ant-design:robot-outlined',
      tooltip: '启动AI检测',
      onClick: () => handleStartAI(record)  // 新增：启动AI任务
    },
    {
      icon: 'ant-design:stop-outlined',
      tooltip: '停止AI检测',
      onClick: () => handleStopAI(record)   // 新增：停止AI任务
    },
    // ... 其他按钮
  ];
  return actions;
};

// 启动AI检测任务
const handleStartAI = async (record) => {
  try {
    createMessage.loading({ content: '正在启动AI检测...', key: 'ai' });
    
    // 1. 生成配置文件
    const config = {
      task_id: parseInt(record.id),
      video: {
        source: record.source,  // RTSP地址
        width: 1920,
        height: 1080
      },
      model: {
        model_path: 'models/SafeHat.onnx',
        confidence_threshold: 0.5
      },
      rtmp: {
        rtmp_url: `rtmp://localhost:1935/live/ai_camera_${record.id}`
      },
      alarm: {
        enable: true,
        hook_url: `http://localhost:48080/admin-api/alarm/callback/${record.id}`,
        confidence_threshold: 0.6,
        cooldown_time: 10
      }
    };
    
    const configRes = await generateConfig(config);
    
    // 2. 启动TASK
    await startTask(parseInt(record.id), configRes.data.config_path);
    
    createMessage.success({ content: 'AI检测已启动', key: 'ai' });
    
    // 3. 更新播放地址为AI推流地址
    record.http_stream = `http://localhost/live/ai_camera_${record.id}.flv`;
    reload();
    
  } catch (error) {
    console.error('启动AI检测失败', error);
    createMessage.error({ content: '启动AI检测失败', key: 'ai' });
  }
};

// 停止AI检测任务
const handleStopAI = async (record) => {
  try {
    createMessage.loading({ content: '正在停止AI检测...', key: 'ai' });
    await stopTask(parseInt(record.id));
    createMessage.success({ content: 'AI检测已停止', key: 'ai' });
    reload();
  } catch (error) {
    console.error('停止AI检测失败', error);
    createMessage.error({ content: '停止AI检测失败', key: 'ai' });
  }
};
</script>
```

**效果**：
- 每个摄像头都有"启动AI检测"和"停止AI检测"按钮
- 点击"启动AI检测" → 自动生成配置 → 启动TASK实例 → 播放地址切换为AI流
- 点击"播放" → 看到带检测框的实时画面

---

**方案2：新建AI任务管理页面**（推荐）

新建文件：`WEB/src/views/ai-task/index.vue`

```vue
<template>
  <div class="ai-task-container">
    <BasicTable @register="registerTable">
      <template #toolbar>
        <a-button type="primary" @click="openConfigModal">
          <template #icon>
            <RobotOutlined/>
          </template>
          新建AI检测任务
        </a-button>
        <a-button @click="reloadAllStatus">
          <template #icon>
            <SyncOutlined/>
          </template>
          刷新状态
        </a-button>
      </template>

      <template #bodyCell="{ column, record }">
        <!-- AI任务状态 -->
        <template v-if="column.dataIndex === 'status'">
          <a-tag :color="getStatusColor(record.status)">
            {{ getStatusText(record.status) }}
          </a-tag>
        </template>

        <!-- FPS显示 -->
        <template v-else-if="column.dataIndex === 'fps'">
          <span>{{ record.fps }} fps</span>
        </template>

        <!-- 操作 -->
        <template v-else-if="column.dataIndex === 'action'">
          <TableAction :actions="getTableActions(record)" />
        </template>
      </template>
    </BasicTable>

    <!-- AI任务配置弹窗 -->
    <AITaskConfigModal @register="registerConfigModal" @success="handleConfigSuccess"/>
  </div>
</template>

<script lang="ts" setup>
import {onMounted, ref} from 'vue';
import {BasicTable, TableAction, useTable} from '@/components/Table';
import {useMessage} from '@/hooks/web/useMessage';
import {useModal} from "@/components/Modal";
import {
  startTask,
  stopTask,
  getTaskList,
  getTaskStatus,
  generateConfig
} from '@/api/task';
import {RobotOutlined, SyncOutlined} from '@ant-design/icons-vue';
import AITaskConfigModal from './components/AITaskConfigModal.vue';

const {createMessage} = useMessage();
const [registerConfigModal, {openModal: openConfigModal}] = useModal();

// 表格列定义
const columns = [
  { title: '任务ID', dataIndex: 'task_id', width: 80 },
  { title: '摄像头名称', dataIndex: 'camera_name', width: 150 },
  { title: '状态', dataIndex: 'status', width: 100 },
  { title: 'FPS', dataIndex: 'fps', width: 80 },
  { title: '处理帧数', dataIndex: 'frame_count', width: 100 },
  { title: '运行时长', dataIndex: 'uptime', width: 100 },
  { title: 'RTMP推流地址', dataIndex: 'rtmp_url', width: 200 },
  { title: '操作', dataIndex: 'action', width: 200 },
];

const [registerTable, {reload}] = useTable({
  title: 'AI检测任务列表',
  api: getTaskList,
  columns: columns,
  pagination: true,
  rowKey: 'task_id',
});

// 获取操作按钮
const getTableActions = (record) => {
  const actions = [];
  
  if (record.status === 'running') {
    actions.push({
      icon: 'ant-design:pause-circle-outlined',
      tooltip: '停止任务',
      onClick: () => handleStop(record.task_id)
    });
  } else {
    actions.push({
      icon: 'ant-design:play-circle-outlined',
      tooltip: '启动任务',
      onClick: () => handleStart(record.task_id)
    });
  }
  
  actions.push(
    {
      icon: 'octicon:play-16',
      tooltip: '播放视频',
      onClick: () => handlePlay(record)
    },
    {
      icon: 'ant-design:setting-outlined',
      tooltip: '配置',
      onClick: () => openConfigModal(record)
    },
    {
      icon: 'material-symbols:delete-outline-rounded',
      tooltip: '删除',
      popConfirm: {
        title: '确定删除此任务？',
        confirm: () => handleDelete(record.task_id)
      }
    }
  );
  
  return actions;
};

// 启动任务
const handleStart = async (taskId: number) => {
  try {
    await startTask(taskId, `config/task${taskId}.ini`);
    createMessage.success('任务启动成功');
    reload();
  } catch (error) {
    createMessage.error('任务启动失败');
  }
};

// 停止任务
const handleStop = async (taskId: number) => {
  try {
    await stopTask(taskId);
    createMessage.success('任务停止成功');
    reload();
  } catch (error) {
    createMessage.error('任务停止失败');
  }
};

// 状态文本和颜色
const getStatusText = (status: string) => {
  const map = {
    'running': '运行中',
    'stopped': '已停止',
    'error': '错误'
  };
  return map[status] || status;
};

const getStatusColor = (status: string) => {
  const map = {
    'running': 'green',
    'stopped': 'red',
    'error': 'orange'
  };
  return map[status] || 'default';
};
</script>
```

**新建配置弹窗**：`WEB/src/views/ai-task/components/AITaskConfigModal.vue`

```vue
<template>
  <BasicModal
    @register="register"
    title="配置AI检测任务"
    width="800px"
    @ok="handleSubmit"
  >
    <BasicForm @register="registerForm" />
  </BasicModal>
</template>

<script lang="ts" setup>
import {BasicModal, useModalInner} from '@/components/Modal';
import {BasicForm, useForm} from '@/components/Form';
import {generateConfig, startTask} from '@/api/task';
import {useMessage} from '@/hooks/web/useMessage';

const {createMessage} = useMessage();
const emit = defineEmits(['success', 'register']);

const [registerForm, {validate, setFieldsValue, resetFields}] = useForm({
  labelWidth: 120,
  schemas: [
    {
      field: 'task_id',
      label: '任务ID',
      component: 'InputNumber',
      required: true,
    },
    {
      field: 'camera_id',
      label: '选择摄像头',
      component: 'Select',
      componentProps: {
        options: [],  // 从摄像头列表获取
        showSearch: true,
      },
      required: true,
    },
    {
      field: 'model_path',
      label: '检测模型',
      component: 'Select',
      componentProps: {
        options: [
          { label: '安全帽检测', value: 'models/SafeHat.onnx' },
          { label: '跌倒检测', value: 'models/FallDetect.onnx' },
          { label: '通用目标检测', value: 'models/yolov11n.onnx' },
        ],
      },
      required: true,
      defaultValue: 'models/SafeHat.onnx',
    },
    {
      field: 'confidence_threshold',
      label: '检测阈值',
      component: 'Slider',
      componentProps: {
        min: 0,
        max: 1,
        step: 0.05,
        marks: {
          0.5: '0.5',
          0.7: '0.7',
        },
      },
      defaultValue: 0.6,
    },
    {
      field: 'enable_alarm',
      label: '启用告警',
      component: 'Switch',
      defaultValue: true,
    },
    {
      field: 'alarm_confidence',
      label: '告警阈值',
      component: 'Slider',
      componentProps: {
        min: 0,
        max: 1,
        step: 0.05,
      },
      defaultValue: 0.7,
      ifShow: ({values}) => values.enable_alarm,
    },
    {
      field: 'cooldown_time',
      label: '告警冷却时间(秒)',
      component: 'InputNumber',
      componentProps: {
        min: 1,
        max: 60,
      },
      defaultValue: 10,
      ifShow: ({values}) => values.enable_alarm,
    },
  ],
  showActionButtonGroup: false,
});

const [register, {closeModal}] = useModalInner((data) => {
  resetFields();
  if (data) {
    setFieldsValue(data);
  }
});

const handleSubmit = async () => {
  try {
    const values = await validate();
    
    // 构建配置
    const config = {
      task_id: values.task_id,
      video: {
        source: values.camera_source,  // 从摄像头信息获取
        width: 1920,
        height: 1080,
      },
      model: {
        model_path: values.model_path,
        confidence_threshold: values.confidence_threshold,
      },
      rtmp: {
        rtmp_url: `rtmp://localhost:1935/live/ai_task_${values.task_id}`,
      },
      alarm: {
        enable: values.enable_alarm,
        hook_url: `http://localhost:48080/admin-api/alarm/callback/${values.task_id}`,
        confidence_threshold: values.alarm_confidence,
        cooldown_time: values.cooldown_time,
      },
    };
    
    // 1. 生成配置文件
    const configRes = await generateConfig(config);
    
    // 2. 启动任务
    await startTask(values.task_id, configRes.data.config_path);
    
    createMessage.success('AI任务启动成功');
    emit('success');
    closeModal();
    
  } catch (error) {
    createMessage.error('配置失败');
  }
};
</script>
```

---

### 🎬 阶段4：视频墙展示（1-2天）

**目标**：同时展示多路AI检测视频

新建页面：`WEB/src/views/ai-task/video-wall.vue`

```vue
<template>
  <div class="video-wall-container">
    <div class="video-wall-header">
      <h2>AI检测视频墙（{{ runningTasks.length }}/50路）</h2>
      <a-radio-group v-model:value="gridLayout" button-style="solid">
        <a-radio-button value="2x2">2x2</a-radio-button>
        <a-radio-button value="3x3">3x3</a-radio-button>
        <a-radio-button value="4x4">4x4</a-radio-button>
        <a-radio-button value="5x5">5x5</a-radio-button>
      </a-radio-group>
    </div>
    
    <div :class="`video-wall-grid grid-${gridLayout}`">
      <div 
        v-for="task in runningTasks" 
        :key="task.task_id"
        class="video-wall-item"
      >
        <!-- 视频播放器 -->
        <Jessibuca 
          :playUrl="task.http_flv_url" 
          :hasAudio="false"
          class="video-player"
        />
        
        <!-- 信息覆盖层 -->
        <div class="video-info-overlay">
          <div class="video-title">{{ task.camera_name }}</div>
          <div class="video-stats">
            <span class="fps">{{ task.fps }} FPS</span>
            <span class="status" :class="task.status">{{ task.status }}</span>
          </div>
        </div>
        
        <!-- 告警标识 -->
        <div v-if="task.has_alarm" class="alarm-indicator">
          <WarningOutlined /> 告警中
        </div>
      </div>
    </div>
  </div>
</template>

<script lang="ts" setup>
import {ref, onMounted, onUnmounted} from 'vue';
import {getTaskList} from '@/api/task';
import Jessibuca from '@/components/Player/module/jessibuca.vue';
import {WarningOutlined} from '@ant-design/icons-vue';

const gridLayout = ref('3x3');
const runningTasks = ref([]);
const updateTimer = ref(null);

// 获取运行中的任务
const fetchRunningTasks = async () => {
  try {
    const response = await getTaskList();
    runningTasks.value = response.data
      .filter(task => task.status === 'running')
      .map(task => ({
        ...task,
        http_flv_url: `http://localhost/live/ai_task_${task.task_id}.flv`,
      }));
  } catch (error) {
    console.error('获取任务列表失败', error);
  }
};

onMounted(() => {
  fetchRunningTasks();
  // 每5秒更新一次状态
  updateTimer.value = setInterval(fetchRunningTasks, 5000);
});

onUnmounted(() => {
  if (updateTimer.value) {
    clearInterval(updateTimer.value);
  }
});
</script>

<style scoped lang="less">
.video-wall-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: #000;
}

.video-wall-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px;
  background: #1a1a1a;
  color: #fff;
}

.video-wall-grid {
  flex: 1;
  display: grid;
  gap: 8px;
  padding: 8px;
  
  &.grid-2x2 { grid-template-columns: repeat(2, 1fr); }
  &.grid-3x3 { grid-template-columns: repeat(3, 1fr); }
  &.grid-4x4 { grid-template-columns: repeat(4, 1fr); }
  &.grid-5x5 { grid-template-columns: repeat(5, 1fr); }
}

.video-wall-item {
  position: relative;
  background: #222;
  border-radius: 4px;
  overflow: hidden;
  
  &:hover .video-info-overlay {
    opacity: 1;
  }
}

.video-player {
  width: 100%;
  height: 100%;
}

.video-info-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
  padding: 8px;
  opacity: 0.7;
  transition: opacity 0.3s;
  
  .video-title {
    color: #fff;
    font-size: 14px;
    font-weight: bold;
  }
  
  .video-stats {
    display: flex;
    justify-content: space-between;
    margin-top: 4px;
    
    .fps {
      color: #4caf50;
      font-size: 12px;
    }
    
    .status {
      font-size: 12px;
      &.running { color: #4caf50; }
      &.error { color: #f44336; }
    }
  }
}

.alarm-indicator {
  position: absolute;
  top: 8px;
  right: 8px;
  background: #f44336;
  color: #fff;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 49% { opacity: 1; }
  50%, 100% { opacity: 0.3; }
}
</style>
```

---

## 📋 最终效果

### 1. 摄像头管理页面 (`/camera`)
- ✅ 管理所有摄像头设备（ONVIF扫描、新增、编辑、删除）
- ✅ 启动/停止RTSP转发（VIDEO模块）
- ✅ **新增：启动/停止AI检测（TASK模块）**

### 2. AI任务管理页面 (`/ai-task`)
- ✅ AI任务列表（50路）
- ✅ 新建AI任务（配置检测模型、阈值、告警规则、ROI区域）
- ✅ 启动/停止/删除任务
- ✅ 实时状态监控（FPS、帧数、运行时长）

### 3. 视频墙页面 (`/ai-task/video-wall`)
- ✅ 同时展示多路AI检测视频（2x2 / 3x3 / 4x4 / 5x5布局）
- ✅ 每路视频显示：摄像头名称、FPS、运行状态
- ✅ 告警高亮显示

### 4. 完整数据流

```
用户操作前端 → TaskManager(7000) → TASK实例(C++) → YOLO推理+画框
                    ↓                       ↓
              生成配置文件            RTMP推流(带检测框)
                                           ↓
                                    ZLMediaKit(80)
                                           ↓
                                    HTTP-FLV播放
                                           ↓
                                    前端视频墙展示
```

---

## 🎯 50路摄像头部署方案

### 硬件要求

| 组件 | 配置 | 说明 |
|------|------|------|
| **CPU** | Intel i7-12700 或更高 | 50路AI推理需要强CPU |
| **内存** | 32GB+ | 每路TASK约500MB，50路需25GB+ |
| **存储** | 500GB SSD | 模型、配置、日志 |
| **网络** | 千兆网络 | 50路视频流带宽需求大 |
| **GPU（可选）** | RTX 4070 或更高 | 加速AI推理（可选，CPU也能跑） |

### 软件架构

```
边缘服务器（一台物理机）
├── ZLMediaKit（流媒体服务器）- 1个实例
├── TaskManager（进程管理器）- 1个实例
│   ├── TASK实例 #1（camera_1）
│   ├── TASK实例 #2（camera_2）
│   ├── ...
│   └── TASK实例 #50（camera_50）
└── PostgreSQL + MinIO + Node-RED（后端服务）
```

**注意**：
- 50个TASK实例都在同一台机器上运行
- TaskManager统一管理50个进程的生命周期
- 每个TASK独立配置文件，独立RTMP流

### 部署步骤

#### Step 1: 启动ZLMediaKit

```bash
cd F:\EASYLOT\zlmediakit
.\MediaServer.exe -c config.ini
```

#### Step 2: 启动TaskManager

```bash
cd F:\EASYLOT\yfeieye-main\TASK\build\Release
.\TaskManager.exe
```

TaskManager会：
- 监听7000端口
- 等待前端发送任务启动请求

#### Step 3: 在前端配置50路摄像头

```typescript
// 批量创建任务示例
const cameras = [
  { id: 1, name: '大门', rtsp: 'rtsp://192.168.1.101:554/stream1' },
  { id: 2, name: '车库', rtsp: 'rtsp://192.168.1.102:554/stream1' },
  // ... 48 more cameras
];

for (let camera of cameras) {
  await generateConfig({
    task_id: camera.id,
    video: { source: camera.rtsp, width: 1920, height: 1080 },
    model: { model_path: 'models/SafeHat.onnx', confidence_threshold: 0.5 },
    rtmp: { rtmp_url: `rtmp://localhost:1935/live/camera_${camera.id}` },
    alarm: {
      enable: true,
      hook_url: `http://localhost:48080/admin-api/alarm/callback/${camera.id}`,
      confidence_threshold: 0.6,
      cooldown_time: 10
    }
  });
  
  await startTask(camera.id, `config/task${camera.id}.ini`);
}
```

#### Step 4: 打开视频墙

访问：`http://localhost:8099/ai-task/video-wall`

选择5x5布局 → 同时看到25路视频（可滚动查看全部50路）

---

## 📊 性能评估

### 单路TASK资源占用

| 指标 | CPU模式 | GPU模式 |
|------|---------|---------|
| **CPU占用** | ~10% (12核心) | ~5% |
| **内存占用** | ~500MB | ~500MB |
| **显存占用** | N/A | ~2GB |
| **推理速度** | 25-30 FPS | 100+ FPS |
| **延迟** | ~1秒 | ~0.5秒 |

### 50路并发预估

| 资源 | CPU模式 | GPU模式 |
|------|---------|---------|
| **CPU占用** | ~80% (12核心) | ~40% |
| **内存占用** | ~25GB | ~25GB |
| **显存占用** | N/A | ~8GB (RTX 4070) |
| **网络带宽** | ~200Mbps (出口) | ~200Mbps |

**结论**：
- CPU模式可以跑50路（但CPU会满载）
- GPU模式更轻松（推荐）

---

## ✅ 验收标准

### 功能验收

- [ ] 单个TASK实例正常运行（RTSP→AI→RTMP→播放）
- [ ] TaskManager成功管理50个TASK实例
- [ ] 前端能通过API启动/停止任意TASK
- [ ] 前端能实时查看所有TASK状态
- [ ] 视频墙能同时播放多路视频
- [ ] 区域告警正常触发（区域内有人→告警，区域外无告警）
- [ ] 告警回调到DEVICE模块，触发Node-RED规则

### 性能验收

- [ ] 单路TASK FPS ≥ 20
- [ ] 50路TASK同时运行，系统稳定
- [ ] 视频延迟 ≤ 2秒
- [ ] 内存无泄漏（24小时运行内存增长 < 10%）
- [ ] CPU占用合理（CPU模式 < 90%）

### 用户体验验收

- [ ] 前端操作流畅（启动任务 < 3秒）
- [ ] 视频墙布局可调整（2x2 / 3x3 / 4x4 / 5x5）
- [ ] 告警实时显示（< 1秒延迟）
- [ ] 日志可查询（Glog输出）

---

## 🔄 后续优化方向

### 短期优化（1-2周）

1. **性能监控面板**
   - 实时CPU/内存/FPS图表
   - 告警统计（每小时/每天）
   - 系统健康度评分

2. **多模型支持**
   - 运行时切换模型（安全帽→跌倒检测）
   - 模型热加载

3. **类别文件加载**
   - 支持中文标签
   - 自定义检测类别

### 中期优化（1个月）

1. **分布式部署**
   - 多台边缘服务器
   - 负载均衡

2. **录像回放**
   - 保存检测视频
   - 事件回放

3. **高级告警规则**
   - 人数统计告警
   - 区域入侵时长告警
   - 跨区域追踪告警

### 长期优化（3个月）

1. **GPU加速完善**
   - 支持CUDA 12.8
   - 或DirectML

2. **云端集成**
   - 边缘-云协同
   - 模型远程更新

3. **智能分析**
   - 行为分析（徘徊、奔跑）
   - 轨迹分析
   - 热力图

---

## 📚 参考文档

- [TASK模块开发指引.md](./TASK模块开发指引.md) - 详细技术实现
- [架构分析与开发路线图.md](./架构分析与开发路线图.md) - 系统架构分析
- [生产环境使用指南.md](./生产环境使用指南.md) - HTTP-FLV部署指南

---

## 🎉 总结

**你的C++ TASK模块 = VIDEO模块 + AI能力**

| 对比项 | VIDEO (Python) | TASK (C++) |
|--------|---------------|-----------|
| 功能 | 仅转发视频 | ✅ AI检测+画框+告警 |
| 性能 | 中等 | ✅ 高性能（C++原生） |
| 管理 | 手动 | ✅ 前端API控制 |
| 并发 | ~10路 | ✅ 50路+ |
| 告警 | ❌ | ✅ 区域检测+规则引擎 |

**开发时间估算**：
- 阶段1（TASK补全）：3-4天
- 阶段2（TaskManager）：2-3天
- 阶段3（前端集成）：2-3天
- 阶段4（视频墙）：1-2天
- **总计：8-12天**

**完成后，你将拥有**：
✅ 前端一键启动50路AI检测任务  
✅ 实时视频墙展示  
✅ 区域告警+规则引擎集成  
✅ 完整的商用级AI视频监控系统  

---

**准备好开始了吗？** 🚀

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 