services:
  taskmanager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taskmanager-server
    ports:
      - "7000:7000"
    volumes:
      - ./config/generated:/app/config/generated
      - ./build/linux/TASK:/app/bin/TASK:ro
    restart: unless-stopped
