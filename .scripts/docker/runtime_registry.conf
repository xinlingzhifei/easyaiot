# =============================================================================
# EasyAIoT 运行时镜像远程仓库（CNB / Docker Registry）
# =============================================================================
# 业务镜像（DEVICE / AI / VIDEO / WEB）的 pull / build-runtime 使用此地址。
# 须以 / 结尾。
#
# 示例:
#   REGISTRY=docker.cnb.cool/soaring-xiongkulu/easyaiot/
#   REGISTRY=docker.cnb.cool/your-namespace/your-project/
# =============================================================================
REGISTRY=docker.cnb.cool/soaring-xiongkulu/easyaiot/
