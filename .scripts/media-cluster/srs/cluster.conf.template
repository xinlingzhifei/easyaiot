# SRS 集群节点配置模板
# 渲染: envsubst < cluster.conf.template > docker.conf
# 变量: MEDIA_NODE_ID, MEDIA_SCHEDULER_HOST, SRS_CANDIDATE_IP

listen              1935;
max_connections     500;
daemon              off;
srs_log_tank        file;
srs_log_file        /mnt/easyaiot-media/logs/srs-${MEDIA_NODE_ID}.log;

http_server {
    enabled         on;
    listen          8080;
    dir             ./objs/nginx/html;
}

http_api {
    enabled         on;
    listen          1985;
    raw_api {
        enabled             on;
        allow_reload        on;
    }
}

stats {
    network         0;
}

rtc_server {
    enabled         on;
    listen          8000;
    candidate       ${SRS_CANDIDATE_IP};
}

vhost __defaultVhost__ {
    tcp_nodelay     on;
    min_latency     on;

    play {
        gop_cache       off;
        queue_length    10;
    }

    http_remux {
        enabled         on;
        mount           [vhost]/[app]/[stream].flv;
    }

    rtc {
        enabled         on;
        rtmp_to_rtc     on;
        rtc_to_rtmp     on;
    }

    dvr {
        enabled             on;
        dvr_path            /mnt/easyaiot-media/playbacks/[app]/[stream]/[2006]/[01]/[02]/[timestamp].flv;
        dvr_plan            segment;
        dvr_duration        60;
        dvr_wait_keyframe   on;
        time_jitter         full;
    }

    http_hooks {
        enabled             on;
        on_publish          http://${MEDIA_HOOK_HOST}:${MEDIA_HOOK_PORT}/video/media/hook/srs/on_publish;
        on_unpublish        http://${MEDIA_HOOK_HOST}:${MEDIA_HOOK_PORT}/video/media/hook/srs/on_unpublish;
        on_dvr              http://${MEDIA_HOOK_HOST}:${MEDIA_HOOK_PORT}/video/media/hook/srs/on_dvr;
    }
}
