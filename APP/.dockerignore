# 依赖目录
node_modules
.pnpm-store
.build-cache
docker-build-logs

# 构建产物（构建阶段不需要，生产阶段会从构建阶段复制）
dist
# dist-prebuilt-full/mini/standard 是本机提取的缓存目录（排除）；
# dist-prebuilt/ 是跨架构构建时 runtime_image.sh 复制进来的目录（必须保留，不能排除）
dist-prebuilt-full
dist-prebuilt-mini
dist-prebuilt-standard

# 压缩文件
dist.zip

# 日志文件
logs
*.log

# 环境文件
# 注意：.env、.env.development、.env.production 等文件需要在构建时使用，所以不能排除
# 只排除本地开发用的覆盖文件（这些文件不应该进入容器）
.env.local
.env.*.local

# IDE配置
.idea
.vscode
*.swp
*.swo
*~

# 锁定文件需打进镜像，供 pnpm install --frozen-lockfile 复现依赖（勿排除）

# Git相关
.git
.gitignore
.gitattributes

# Docker相关
Dockerfile
docker-compose.yaml
.dockerignore

# 文档
README.md
README_en.md
TROUBLESHOOTING.md
*.md

# 测试文件
**/__tests__/**
**/*.test.*
**/*.spec.*

# 其他
.DS_Store
Thumbs.db
*.zip
*.tar.gz

