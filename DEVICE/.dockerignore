# Git 相关
.git
.gitignore

# IDE 相关
.idea
.vscode
*.iml
*.ipr
*.iws

# 日志文件
*.log
logs/

# 临时文件
*.tmp
*.temp
*.swp
*.swo
*~

# 文档
*.md
README*
LICENSE

# 脚本文件（不需要在容器中）
*.sh

# Docker 相关
docker-compose.yml
Dockerfile
Dockerfile.base
docker-bake.hcl
.dockerignore

# 构建缓存（Maven 经 --build-context maven-repo 挂载，勿打进 COPY 上下文）
.build-cache

# 宿主机 Maven 构建产物（防止陈旧 target/classes 被打进镜像内 mvn package 的 jar）
# 注意：根目录 target/jars 是第二阶段运行时镜像的输入，不能排除
iot-*/target
iot-*/*/target

# 其他
.DS_Store
Thumbs.db

