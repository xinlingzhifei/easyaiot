spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 25MB
      max-request-size: 30MB
  application:
    name: node-server

  profiles:
    active: local

server:
  port: 48094

logging:
  file:
    name: ${user.home}/logs/${spring.application.name}.log
